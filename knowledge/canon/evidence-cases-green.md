---
title: 초록불이 거짓인 경우
tier: reference
applicability: always
sync_to_siblings: true
version: 1.0.0
last_updated: 2026-08-23
---

# 증례 — 초록불이 거짓인 경우

게이트가 초록인데 아무것도 검사하지 않았거나, 틀린 것을 정답으로 삼았거나, 가장 싼 통과 경로가 의도한 작업이 아니었던 실측 사례.

> **이 파일은 규범이 아니라 증거다.** 규범은 `.claude/rules/agent-evidence.md` 에 있고,
> 그 규칙의 각 항목이 여기 사례를 가리킨다. 2026-08-23 에 `agent-evidence.md` 가
> **137KB** 로 자라 매 턴 30+ repo 에 주입되고 있었기에 **원문 그대로** 분리했다 —
> 요약하면 측정값이 죽는다. 규범은 주입되고, 증거는 필요할 때 읽는다.

---

## 초록불 주장 — **무엇이 통과했나가 아니라 무엇을 실제로 검사했나** (MUST)

게이트가 초록이라는 것은 **"검사가 통과했다"** 는 뜻이지 **"그것이 검사됐다"** 는 뜻이 아니다.
빈 대상 · 좁은 스코프 · 관대한 스텁 · 잘못된 입력은 **전부 통과로 위장한다.**

실사례 (modfolio-design 2026-07-27 실측 4건 + 이 저장소 실측):

| 무엇이 초록이었나 | 실제로 검사한 것 |
|---|---|
| axe "위반 0" | 컨텍스트를 `document.body` 로 줘 `landmark-one-main`·`page-has-heading-one` 이 **스코프 밖**. h1 이 없는 페이지가 통과 |
| 배포 검증 "이상 없음" | `preload` 제거로 스캔 대상이 **0건**이 됐는데 0건을 통과로 읽음 |
| 로컬 테스트 통과, 프로덕션 500 | 스텁이 **가변** Response 를 반환, 실제 바인딩은 **불변** |
| 4스택 parity 통과 | 픽스처가 prop **타입**을 몰라 넷이 **똑같이 틀렸다** |
| "quality:all 1054 pass" | `grep "N pass"` 는 **테스트 개수**이지 게이트 결과가 아니다. `check` 가 exit 1 인데 `&&` 로 중단돼 이전 실행 숫자를 읽고 green 보고 |
| 게시 성공 | 백필 실패가 한 줄 warn 이고 **이유가 잘려**(npm 매니페스트가 800자 예산을 다 씀) 매 릴리즈 조용히 실패 |

### 두 번째 부류 — 대상도 있고 검사도 도는데 **기준이 틀린** 경우 (2026-07-27 인바운드)

위 표는 전부 "검사 대상이 비었거나 좁거나 가짜" 였다. 다음은 다르다 — **대상은 진짜고 검사도
정상 실행된다. 정답으로 삼은 것이 결함이다.** 그러면 게이트는 결함을 *지킨다*.

| 무엇이 초록이었나 | 정답으로 삼은 것 | 출처 |
|---|---|---|
| 결제 화면 시각 회귀 통과 | 베이스라인 PNG 가 **"500 / Payment configuration error"** 화면. 에러를 에러와 비교하며 계속 초록 — 나중에 진짜 수정이 **회귀로 잡힌다** | pay |
| `client_credentials` 스코프 스펙 통과 | 스펙 문장이 *"unrestricted key(no scopes) 는 요청 스코프를 허용한다"* — **요구사항이 아니라 구현을 보고 쓴 테스트**. fail-open 이 명세가 됐다 | connect |
| `createApiKey` 유닛 스펙 통과 | **스펙을 소비자로 셌다.** 실제 프로덕션 호출자는 0(`api_key` 0행) — 기능은 완결됐고 **문이 없었다** | connect |
| 프로필 페이지 정상 | UI 가 `<span>Verified</span>` **리터럴**. 읽지도 않은 상태를 단언 → 없는 "인증" 버튼이 필요 없어 보였다 | connect |
| a11y 22/22 통과 | 런타임이 `each_key_duplicate` 를 **던지고** 있었고 그 예외를 보는 단언이 없었다 | pay |

**공통 형태**: 검사는 성실히 도는데 *무엇이 옳은가*를 산출물·구현·표시문자열에서 역산했다.
산출물에서 명세를 역산하면 **결함이 명세가 된다** — 허브가 "게이트가 저자 의도가 아니라
포매터 출력을 검사하고 있었다" 로 겪은 것과 같은 뿌리다(connect 가 그 연결을 지적했다).

### 세 번째 부류 — **검사를 만족시키는 가장 싼 방법이 의도한 작업이 아닐 때** (2026-08-03 modfolio-pay)

앞 두 부류는 *대상*이나 *정답*이 틀렸다. 이건 다르다 — 대상도 정답도 맞는데 **통과
조건이 저자의 의도가 아니라 그 의도의 가장 싼 대리 지표**다. 그래서 이 게이트들은
**정직하게 실패도 한다**(빈 스캔도 0건도 아니다). 작동하는 게이트처럼 보이는 것이
이 부류의 특징이다.

- **"그 파일이 X 를 언급하는가" 는 언급만으로 통과한다.** 실측: 아키타입 계약 검사가
  `expect(source).toContain('archetype/…svelte')` 하나였다 — 24화면에 import 21줄만
  붙이면 **화면을 한 픽셀도 안 고치고** 초록이 된다. 하필 *"아무도 import 하지 않으면
  그건 주장이지 형태가 아니다"* 를 막으려고 만든 게이트 안에 있었다. → 검사를 쓸 때
  **"이 조건을 만족시키는 가장 싼 방법이 무엇인가"** 를 묻고, 그게 의도한 작업이
  아니면 **렌더/런타임 짝**을 붙인다(마크업에 실제로 있는가 · 라이브 DOM 에 있는가).
  ⚠ biome 의 unused-import 는 `.svelte` 템플릿 안을 보지 않아 import 만 남기는 것이
  실제로 가능하다.
- **`var(--x, fallback)` 은 아무도 `--x` 를 안 줘도 에러가 없다 — 기능만 사라진다.**
  실측: `.mp-stagger` 가 자식마다 `--i * 40ms` 지연을 주는데 **앱 어디에도 `--i` 를
  넣는 코드가 없어** 전 자식이 0ms 였다. 고장처럼 안 보이고 **그냥 페이드로 보인다.**
  직전 인계 문서가 *"이 동작을 조용히 잃지 마라"* 라고 경고하고 있었는데 **일어나지도
  않던 동작을 보호하고 있었다.**
  ⚠ **다만 SET 이 0건이라고 전부 결함은 아니다** (atelier 2026-08-04, 이 스윕을 자기
  저장소에 돌려 보고 정정). **폴백이 무엇을 낳는가**로 갈린다:

  | 폴백이 낳는 것 | 판정 |
  |---|---|
  | **선언된 동작의 부재** — 지연·오프셋·애니메이션이 «그냥 안 일어남» | **결함.** 고장처럼 안 보이고 «원래 그런 디자인» 으로 보인다 (pay `--i`: 전 자식 0ms) |
  | **올바른 기본값** — 훅은 오버라이드용이고 폴백이 곧 정답 | **정상** (atelier `--pill-bd`: 폴백이 그 앱의 기본 테두리색. 랜딩에선 실제로 SET 한다) |

  판별 질문: ***"이 프로퍼티를 아무도 안 주는 지금, 사용자가 보는 것이 의도된 상태인가?"***
  이걸 안 붙이면 스윕이 **미사용 확장점을 전부 결함으로 보고**하고, 오버라이드 훅에 폴백을
  두는 **좋은 CSS 관행을 벌한다**(「과탐이 규약 준수를 벌한다」와 같은 축). → 클래스 유무가 아니라 **계산된 값**(`animation-delay`
  등)을 단언한다. 스윕 한 줄: `rg -o -- '--[a-z-]+, *[0-9a-z]' src | sort -u` 로
  폴백 있는 커스텀 프로퍼티를 뽑고, 각각을 **SET 하는 곳이 있는지** 확인.
- **사용자에게 스위치를 주고 아무도 읽지 않는다.** 실측: 알림 설정이 토글 10개를
  보여주고 저장하고 다시 그렸는데 **보내는 쪽은 한 번도 읽지 않았다** — "결제 완료"
  를 꺼도 메일이 그대로 나갔다. 10개 중 2개는 **끌 대상 자체가 없었다**(템플릿·호출부
  0). 아무것도 실패하지 않은 이유: UI 쪽과 발신 쪽이 **각자 내부적으로는 일관**됐고
  둘이 만나는 지점이 없었다. → 키 목록을 하나로 모으고 **양방향 JOIN 게이트**:
  스위치는 있는데 게이트가 없으면(=장식) 실패, 게이트는 있는데 스위치가 없으면(=끌 수
  없음) 실패. **동작하는 옵트아웃을 보여주고 무시하는 것은 기능 누락보다 나쁘다 —
  사용자에게 사실이 아닌 것을 말한 것이다.**
- **`fullPage: true` 가 app-shell 레이아웃에서는 무동작이다.** `height: 100dvh` +
  안쪽 `overflow:auto` 면 **문서가 절대 스크롤되지 않아** 늘릴 높이가 없다. 실측:
  베이스라인 17개 전부가 정확히 뷰포트 크기였고 **접힌 윗부분만 검증하면서 "전체"라고
  주장**하고 있었다(스펙 주석이 *"long-page 도 전체 캡쳐"* 라고 적어 둔 채). 확인법:
  산출물 PNG 높이가 전부 뷰포트 높이와 같으면 `fullPage` 가 안 먹고 있는 것이다.

추가로 같은 날 올라온, **검사가 아예 안 돌던** 형태 셋:

- **테스트 스크립트가 디렉터리로 좁혀져 있었다** — `"test": "bun test server"`. `server/` 밖
  테스트는 작성해도 **러너가 보지 않는다.** "검사가 약한" 게 아니라 **없는** 것이라 더 위험하다.
  새 테스트를 넣었는데 총계가 안 늘어서 발견됐다 (atelier)
- **게이트 트리거 목록에 그 게이트 자신이 없었다** — a11y 트리거가 `apps/app/src/**` 만 봐서
  a11y 스펙·fixture·config 를 고치는 push 는 **그 스위트를 안 돌리고** 나갔다 (pay)
- **마스크 셀렉터가 0개를 매칭하면 조용히 무력화**되고 스크린샷은 계속 초록이다가 어느 날
  갑자기 깨진다 — 셀렉터가 실제 요소를 잡는지 확인하는 테스트가 따로 필요하다 (pay)

### 요구

- **게이트 결과는 exit code 로 판정한다.** 출력 문자열(`N pass`, `no errors`)은 결과가 아니다.
  `bun run <gate> >/dev/null 2>&1; echo $?`
  ⚠ **판정할 명령이 복합문의 마지막이 아니면 종료코드는 남의 것이다.** 파이프만의 함정이
  아니다 — `;` · `tee` · `|| true` · `&& echo ok` 가 전부 같은 축이다(connect 2026-08-03 이
  파이프 규칙을 일반화해 보내왔다). 세 얼굴:
  - `bun run gate | tail -5; echo $?` → `tail` 의 0 (pay 2026-07-28)
  - `git push > log 2>&1; echo "EXIT=$?" >> log` → **`$?` 는 맞게 찍히는데 그 명령 자체의
    종료코드는 `echo` 의 0 이다.** 하네스·CI 가 그걸 보면 **실패를 성공으로 읽는다** —
    connect 는 그렇게 실패한 푸시를 "completed" 알림으로 받았고 로그 안의 `PUSH_EXIT=1` 을
    못 봤다. 사람이 읽는 값과 기계가 읽는 값이 갈린다.
  - `cmd 2>/dev/null | head || fallback` → `||` 가 파이프의 0 을 봐서 **폴백이 안 돈다**.
    남는 건 빈 출력이고 그게 "델타 없음" 으로 읽힌다(athsra 2026-08-03, 아래 「멤버가 못 도는
    MUST」와 겹쳐 발생).

  판정할 명령을 파이프·후행절 없이 돌리거나 `PIPESTATUS`/`pipefail` 을 읽어라.
- ⚠ **`cmd | grep -q` 는 «매치했는데 안 한 것처럼» 동작할 수 있다 — 그리고 검사가 반대로
  붙어 있으면 그게 «거짓 초록»이 된다** (허브 2026-08-05 자기 스크립트에서 실측).
  `set -o pipefail` 아래서 `grep -q` 는 **첫 매치에서 즉시 종료**한다. 왼쪽 명령이 아직 쓸
  것이 남아 있으면 **SIGPIPE 로 죽어 파이프라인 상태가 141** 이 되고, `&&` 는 실패한다.

  ```
  OUT="Feedback: written to …/node_modules/…"$'\n'"<300KB>"
  echo "$OUT" | grep -q "written to.*node_modules"   → 파이프라인 상태 141
    && note_fail …   ← 실행 안 됨
    || note_ok  …    ← **실행됨. 진짜 결함이 초록으로 보고된다**
  ```

  이 저장소의 published-canary 가 정확히 그 형태였고, 하필 **거짓 초록 쪽**이 2026-07-27
  실사건(매니페스트가 `node_modules` 안에 쓰여 다음 설치에 지워짐) 때문에 만든 체크였다.
  같은 파일의 다른 두 곳은 반대로 붙어 있어 **거짓 빨강**이 된다 — 매 릴리즈 반복되면
  사람을 «무시하도록 훈련»시킨다(infra: *"거짓 음성은 노이즈보다 나쁘다"*).

  → **처방**: 문자열 판정에 파이프를 쓰지 않는다. 셸 내장 `case "$OUT" in *"…"*)` 는
  서브프로세스도 파이프도 없어 이 창이 아예 없다. `grep` 이 필요하면 `grep -c`/출력을
  변수에 받아 **값으로** 판정한다.

  ⚠ **측정 없이 원인으로 쓰지 말 것.** 허브는 이 형태를 고치던 중 실제로 겪은 1회성
  `✗ sync reported errors`(출력에 `0 errors` 가 있었다)를 이걸로 설명하려다 멈췄다 —
  실측하니 그 출력은 **2915B 이고 매치 위치가 2832B**, 파이프 버퍼(64KB) 안이라
  **SIGPIPE 가 날 수 없었다**(파이프라인 상태 0). 수정 후 3/3 초록이지만 그 1회는
  **원인 미상으로 남는다.** 「재현되지 않는 실패는 «없던 일» 이 아니라 원인 미상」이
  자기 수정에도 적용된다 — **내가 방금 고친 것이 그 증상의 원인이라는 보장은 없다.**
- **개수를 계산해 출력까지 하고 판정에는 안 쓰는 스크립트가 압도적으로 흔하다**(athsra 2026-07-28
  — 게이트 전수 감사 7건 중 **6건**이 이 형태). 감사 질문 하나로 대부분 걸린다:
  *"이 스크립트가 출력하는 숫자가 exit code 에 들어가는가?"* 수정도 대개 한 줄이다.
- **문자열 앵커는 주석을 먼저 걷어내고 선언 형태로 좁힌다**(athsra) — `css.indexOf(':root')` 가
  헤더 주석의 *언급*을 앵커로 잡아 우연히 맞고 있었고, 새 가드가 한 번도 발동하지 못했다.
  음성 대조가 아니었으면 "가드 추가 완료"로 끝났을 자리다. 앵커는 `:root {` 처럼 선언만 매칭.
- **읽기 실패를 판정값으로 환원하지 않는다**(athsra) — `catch { return false }` 가 "CSS 파일
  없음"을 "다크에서 ink 안 바뀜"으로 바꿔, 스캔에서 빠진 앱이 **의도된 제외처럼** 보고됐다.
  읽기 실패는 별도 상태(판정 불능)로 흘려라 — 위 "판정 불능과 위반은 다른 종료 코드" 와 같은 축.
- **빈 대상은 통과가 아니라 실패다.** 스캔·매칭·수집이 0건이면 그 자체를 에러로 만든다.
  "0건이라 위반이 없다" 와 "0건을 대상으로 검사했다" 는 다른 문장이다.
  ⚠ **간접 형태 — 판정 «기준» 이 0 이 되는 경우가 더 안 보인다** (atelier 2026-08-04).
  게이트가 **외부 설정을 읽어** 커버리지를 판정하면(`testMatch`·`include`·`files`),
  **«못 읽음» 을 «없음» 으로 접는 순간 기준 집합이 비고 전부 통과**한다. atelier 실측:
  playwright `testMatch` 파싱 실패 → `matches.length === 0` → **모든 테스트가 커버됨**으로
  판정. 대상이 0 이 되는 게 아니라 **잣대가 0** 이 되는 것이라 「빈 대상」 검사에 안 걸린다.
  → **선언 수와 파싱 수를 비교**해 다르면 **판정 불능(2)**. 값을 해석하려 들지 않는다 —
  정규식·글롭·배열·함수 무엇이든 올 수 있고, 잘못 해석하면 «커버된다» 는 **거짓 결론**이 된다.
  그 설정이 하나뿐이라는 **전제 자체도 검사**한다(0개·2개 → 2).
  ⚠ 이건 **합성 대조로 안 걸린다** — 합성자는 `testMatch` 를 **읽을 수 있는 형태로** 심는다.
- **스텁은 프로덕션보다 관대하면 안 된다.** 그리고 **스텁이 실제 조건을 흉내내는지 자체를 단언**한다.
  ⚠ **그런데 관대함은 «응답» 만의 축이 아니다 — 모의 서버는 «무엇을 돌려주나» 로 설계되고
  «무엇을 받아들이나» 로는 설계되지 않는다** (athsra 2026-08-04). 실측: 브라우저 확장이
  유닛 **79** + 실제 크롬 e2e **7**, 전부 통과. 그런데 확장이 부르는 경로 3개가 워커에
  **하나도 없었다**(`/v1/secrets/:project/envelope` 등 — 워커 전체에 `envelope` 을 경로
  세그먼트로 갖는 라우트가 **0개**). 읽기도 쓰기도 전부 404 여서 **그 확장의 존재 이유가
  통째로 동작하지 않았다.** 통과한 이유: e2e 스텁이 **아무 경로에나 답했다.**
  응답 *내용*은 문제가 없었다 — **스텁이 주소를 안 봤다.**
  그래서 초록이 *"이 클라이언트는 동작한다"* 로 읽혔지만 실제로 검증된 것은 폼 감지·채우기
  같은 **메커니즘**뿐이었다. → 스텁은 **모르는 경로에 404 를 내야** 한다. 그리고 대조는
  **유한한 쪽에서** 출발한다(워커 라우트 목록 → 클라이언트가 부르는 경로).
- **검사 표면이 의도한 범위를 덮는지 먼저 확인한다** — 스코프·컨텍스트·glob·`files` allowlist.
  ⚠ **«매 커밋 검사한다» 는 «저장소를 검사한다» 가 아니다** (athsra 2026-08-04).
  diff 게이트(`--staged`·changed-files-only)는 **이미 들어와 있는 것에 대해 원리적으로
  침묵**한다 — 훅이 생기기 전에 커밋된 것은 **영원히 재검사되지 않는다.** 게이트는 성실히
  돌고 매번 초록인데 검사 표면이 「이번 diff」로 좁혀져 있다. 실측: 시크릿 스캐너를
  pre-commit 에만 걸어 둔 저장소를 **전수로 한 번 돌리자 즉시 high 1건**(2개월 전 커밋된
  테스트 픽스처의 RSA 개인키). 사고는 아니었지만 **2개월간 아무도 다시 본 적이 없다는 것**이
  결함이다. ⚠ 그 저장소는 그 스캐너를 **만들어서 다른 repo 에 파는 곳**이었다.
  → diff 게이트 옆에 **전수 스윕을 주기적으로** 둔다(또는 CI 에 한 벌).
- **`.gitignore` 는 git 을 가릴 뿐, 파일시스템을 훑는 도구는 못 가린다 — 그래서 검사가
  *덜* 보는 것만이 아니라 *더* 보는 것도 결함이다.** 앞 항목이 "스코프가 좁아 놓친다" 라면
  이건 반대 방향이다. 실측 2026-08-03(허브): 벤치가 `git worktree` 사본을 `.evolve-state/`
  (gitignored) 안에 만들었더니, 저장소를 전수 스캔하는 거버넌스가 **사본 속
  `scripts/modfolio/governance.ts`** 를 진짜 소스로 읽었다. 하필 그 파일은 자기 탐지
  정규식(`/eval\s*\(/`)을 리터럴로 갖고 있어 **ASI05 eval() RCE critical** 이 떴고
  `release:gate` 가 173/174 로 떨어졌다. self-reference 제외는 **경로 기반**이라 사본
  경로에는 안 걸린다. → **저장소 안에 저장소 사본(worktree·clone·fixture 트리)을 두지
  않는다.** 근본 수정은 스캐너에 예외를 추가하는 것이 아니다 — 예외는 그 스캐너 하나만
  덮고, audit·시크릿 스캔·문서 자격증명 검사 등 **다른 모든 전수 검사에 같은 지뢰를
  남긴다.** 임시 트리는 저장소 밖(`tmpdir()`)에 만든다.
- **문서·선언은 필요조건이 아니라 충분조건이어야 한다 — "이대로만 하면 되는가"를 기계로 묻는다.**
  실측 2026-07-28(design): 컴포넌트가 `[role="radio"]` 자식을 요구한다고만 선언했는데
  `aria-checked` 는 role=radio 의 **필수** 속성이라, **선언을 그대로 따른 앱이 critical 위반을
  실어 나른다.** → 선언한 조건**만** 만족시킨 최소 마크업으로 게이트를 돌려라. 런타임이
  정규화해 주더라도 **SSR 이 내보내는 HTML 은 앱 마크업 그대로**이므로, 못 고치는 상태가
  있으면 **선언이 그 사실을 말해야 한다.**
- **설정이 둘로 나뉘면 어느 쪽에서도 검사되지 않는 파일이 생긴다.** tsconfig · vitest project ·
  eslint override — **"각 설정이 통과한다"와 "모든 파일이 어딘가에서 검사된다"는 다른 문장이다.**
  잘못된 쪽으로 간 파일은 시끄럽게 실패하지만, **어느 쪽에도 안 잡힌 파일은 두 게이트가 조용히
  성공한다**(design 실측: glob 파티션 → 타입검사 0인 파일). → 파티션이 전체를 정확히 한 번씩
  덮는지 자체를 테스트로 잠근다.
- **판정 불능과 위반은 다른 종료 코드여야 한다.** 같은 코드로 내면 CI 가 *"검사가 성립하지
  않았다"* 를 *"위반이 없다"* 로 읽는다.
  ⚠ **그리고 이 규칙은 데이터 층에도 내려온다** (athsra 2026-08-04) —
  **«읽지 못했다» 와 «비어 있다» 를 같은 값으로 표현하면, 그 값을 쓰기에 넘기는 순간
  되돌릴 수 없다.** 파서가 손상 입력을 빈 값으로 degrade 하는 것은 **읽기에서는 옳다**
  (가용성 — 한 항목 손상이 전체를 못 열게 만들지 않는다). 그런데 흔한 관용구가
  `parse → 수정 → serialize → PUT` 이라 **저장 한 번에 원본 전체가 방금 만든 것 하나로
  덮인다.** athsra 실측: 손상 볼트 → parse 가 본 기존 항목 **0**(실제로는 «읽지 못한» 것)
  → 저장 후 **1**, 나머지 소멸. **사용자가 보는 것은 «저장 성공» 뿐이다.**
  이 형태는 **읽기 쪽만 보면 절대 안 보인다** — degrade 가 정답이고 테스트도
  *"손상돼도 앱이 안 죽는다"* 를 확인하며 초록이다. **쓰기를 붙여야 드러난다.**
  → 처방: degrade 한 값에 **«읽지 못함» 표식**을 달아 쓰기 경로가 거부하게 하거나,
  최소한 **빈 결과일 때 쓰기를 건너뛴다**. 허브 `init-lock.ts` 가 후자다 —
  `locked.length === 0` 이면 `skipped-empty` 로 **안 쓴다**(2026-08-04 이 축으로 재확인). 실측 2026-07-28(design): `design-lint srcc`(경로 오타)가
  **0 files · 0 errors · exit 0** — 오타 하나로 CI 가 영구 초록이었다. 위반=1 · 사용 오류/대상
  0건=2 로 분리한다(허브도 같은 날 `canary:published` 의 "런타임 부재" 를 통과에서 실패로 옮겼다).
- **검사 대상이 아예 아닌 표면이 있다 — 그리고 그게 가장 오래, 가장 널리 틀린다.**
  코드는 타입·스키마가 지키고 매니페스트는 테스트가 지키지만, **사람이 읽고 그대로 구현하는
  공개 문서는 아무 게이트도 보지 않는다.** 실측 2026-07-28(pay): `pay.modfolio.io/docs/webhooks`
  가 **존재한 적 없는 헤더 2종**과 **없는 이벤트 4종**을 광고하고 실제 발행 18종 중 8종을
  누락했으며, 검증 예제는 CF Workers 에서 안 도는 `node:crypto` 였고 재시도 간격은 허구였다.
  그리고 **modfolio-press 에 그 없는 이벤트(`payment.refunded`)의 핸들러가 실재한다** —
  이름이 우연히 겹칠 확률보다 문서를 보고 짰을 확률이 높다. 즉 문서는 품질 문제가 아니라
  **하류 결함을 생산**했다. → 발신 목록·헤더명·에러코드를 갖는 공개 문서는 **발신 코드와
  양방향으로 대조하는 테스트**를 붙인다(정규식이 0건을 잡으면 실패하는 공허성 가드 포함).
  **세 번째 표면 — DB 스키마 주석**(athsra 2026-08-04). 웹훅 CLI 도움말과 **D1 스키마
  주석**이 `--events=put,unset,delete,rollback,restore` 를 광고하는데 `secrets.unset` **발행처가
  0건**이었다(클라이언트가 읽고-고쳐-PUT 하므로 서버는 PUT 만 본다). `--events=unset` 만
  구독한 사람은 **영원히 아무것도 안 받고 왜인지 알 방법도 없다** — 200 도 에러도 아닌 침묵이다.
  ⚠ **스키마 주석은 공개 문서보다 조용하고 더 정확히 복제된다** — 개발자가 *"이 컬럼에 뭘
  넣지"* 할 때 읽는 자리라 **표준 예시가 곧 사용자가 복사하는 값**이다.
  그리고 **반대 방향이 더 나빴다**: 발행되는데 표에 없던 것이
  `secrets.{rollback,restore}.forced` 계열 — **전부 안전 게이트를 우회한 작업**이다.
  보안 모니터링으로 웹훅을 붙인 사람이 가장 알아야 할 사건인데 **정확히 그것만 안 갔다.**
  정상 롤백은 알림이 오고 강제 롤백은 안 오는, **방향이 뒤집힌** 상태였다.
  **두 번째 사례 — 트랜잭션 이메일**(2026-08-03 atelier-and-folio). 한 파일에서 5건이
  나왔고 **전부 프로덕션 발송 중**이었다. 소스 게이트도 빌드 산출물 대조도 axe 도 e2e 도
  그 파일을 본 적이 없다. 여기서 하류는 문서를 읽는 개발자가 아니라 **고객**이다:
  없는 상품("레벨 테스트")을 신청했다고 말하고, 통화 시간이 랜딩 5곳(30분)과 달랐고
  (25분), 지킬 수 없는 SLA("48시간 내")를 약속했고, 등록 상호를 손으로 적어 갈랐다.
  ⚠ **가장 조용한 건 `reply_to` 누락이었다** — Resend 는 검증된 **도메인**이면 어떤
  로컬파트로도 보내므로 `noreply@` 발송은 **200 을 받는다.** 수신함이 없다는 사실은
  **어느 로그에도 안 남고 답장만 사라진다.** 전송 성공 코드가 도달 가능성을 뜻하지
  않는다. → 고객에게 나가는 문자열(이메일·SMS·푸시 본문)은 랜딩·상품 정의와 **같은
  출처에서 파생**시키거나, 최소한 숫자·상호·상품명을 대조하는 테스트를 붙인다.
- ⚠ **«이 도구가 X 를 못 본다» 는 X 가 있는 상태에서 재야 한다.** 도구의 한계를 기록하는
  일은 **그 자체가 측정**이고, 대상이 없는 상태에서 잰 0건은 한계의 증거가 아니다
  (고친 뒤·제거한 뒤·설정을 바꾼 뒤). 이 형태가 특히 위험한 이유는 **기록이 다음을
  오염시키기** 때문이다 — *«이 도구는 어차피 못 본다»* 는 한 줄이 **다음 진짜 0건도
  설명해 버린다.** 실측 사례는 아래 「판단 근거를 자르지 않는다」에 있다(2026-08-04
  athsra: 게이트 문서에 **존재하지 않는 한계**를 적기 직전이었다). 허브 양성 대조:

  ```
  fast-uri 3.1.4  (dependency)     → exit 1 · ">=3.0.0 <3.1.5" 로 정확히 보고
  fast-uri 3.1.4  (devDependency)  → exit 1 · 동일
  fast-uri 3.1.5                   → exit 0 · "No vulnerabilities found"
  ```
- ⚠ **판단 근거로 쓸 출력을 `head`·`tail`·`grep` 으로 자르지 않는다** (athsra 2026-08-04,
  하룻밤에 **네 번**). 이건 위 「truncated diff」의 일반화다 — 그 항이 diff 절에 있어서
  **diff 이야기로만 읽혔고**, 그래서 같은 실수가 diff 밖에서 반복됐다.

  | 자른 것 | 잘못 내릴 뻔한 결론 |
  |---|---|
  | CI 실패 로그 `head -70` | E2E 실패 **7건 중 4건**만 보고 «다 고쳤다» |
  | `bun audit \| grep … \| head -6` | **«`bun audit` 이 이 권고를 원리적으로 못 본다»** — 앞 6줄이 다른 패키지로 차 있었고 `fast-uri` 줄은 그 아래였다. 게이트 문서에 **없는 한계**를 적기 직전 |
  | CI 로그 그룹 목록 `tail -22` | «내 새 스텝이 CI 에서 안 돌았다» |
  | `catch → 빈 값` 스윕 정규식 | 여러 줄 `catch` 를 못 봐 **0건 → «해당 없음»** (다시 짜니 **40건**) |

  넷 다 **작은 수나 0건을 «없다» 로 읽을 뻔했다.** 「0건이면 probe 부터 의심한다」가 매번
  구했지만 **적용이 매번 한 박자 늦었다** — 그러니 규칙은 «의심하라» 가 아니라
  **«애초에 자르지 마라»** 여야 한다. 출력이 길면 자르지 말고 **`> log` 로 남기고 전체를 읽는다.**

  ⚠ 그리고 이건 **남의 노트를 읽을 때도** 적용된다(허브 2026-08-04 자기보고): 나는 athsra
  노트의 **서술 순서**(§1 수정 → §2 «지금 실측»)를 **실행 순서**로 읽고 *"고친 뒤에 쟀다"*
  로 원인을 단정했다. 실제 실행 순서는 반대였고 원인은 `head -6` 이었다. **문서의 절 번호는
  타임스탬프가 아니다** — 순서를 근거로 삼으려면 그렇게 적혀 있는지 확인하거나 물어본다.

  ⚠ **그리고 자르는 것이 내가 아닐 수 있다 — 서버가 자른다** (허브 2026-08-08 실측,
  자기보고). 위 항들은 전부 «내가 `head` 를 붙였다» 였다. 페이지네이션은 다르다:

  ```
  GET /api/v1/orgs/modfolio/repos            → 저장소 30개   ← 이것을 «전부» 로 읽었다
  GET /api/v1/orgs/modfolio/repos?limit=100  → 64개
  ```

  **200 이고, JSON 이 온전하고, 어디에도 «더 있음» 이라고 쓰여 있지 않다.** 그래서
  「자르지 마라」를 지켜도 걸린다 — 자를 곳이 없었기 때문이다.

  대가는 이 문서가 이미 아는 형태로 나왔다: 잘린 목록으로 *"NAS 에 저장소 5곳이 없다"*
  를 판정하고 **형제 repo 에 없는 작업을 요청하는 문서를 보냈다.** 다섯 곳 전부 실재했고
  `pushurl` 배선까지 돼 있었다(철회 = `feedback/modfolio-infra/…-correction.md`).

  → **목록을 세는 API 는 «전부인가» 를 명시적으로 묻는다** — `limit` 을 최대로 주거나,
  `total_count`/`Link: rel="next"` 헤더를 읽거나, **받은 개수가 한계값과 정확히 같으면
  판정 불능**으로 흘린다(30개를 받았는데 기본 한계가 30이면 그건 «30개» 가 아니다).
- ⚠ **표시용으로 자른 문자열이 판정 입력이 되면, 멀쩡한 것이 «부재» 로 보고된다**
  (visualize 2026-08-05). 이 문서의 「판단 근거를 자르지 않는다」는 **사람이 읽는 출력**을
  다뤘다. 이건 그 안쪽 — **코드가 자기 판정에 쓰는 값**이 이미 잘려 있는 경우다.

  ```ts
  return { code, out: `${out}${err}`.trim().slice(0, 200) };   // ← 표시 관심사
  ```
  ```
  docker ps --format '{{.Names}}'   실제 68개(약 1,200자)
    → .slice(0, 200)                20개에서 절단, 20번째가 «visualize-serv…»
    → names.includes('visualize-server')   false
    → ✗ "visualize-server 부재 (20개 실행 중)"
  ```

  ⚠ **하필 형제 배포 직후라 「남의 `--remove-orphans` 가 우리 컨테이너를 지웠다」는
  일어나지 않은 사고를 보낼 뻔했다.** 거짓 빨강이 **엉뚱한 repo 에 대한 경보**로 번지는
  형태다(허브도 같은 날 「경보 전 반증도 실물로」를 다른 경로로 배웠다).

  → **처방**: 실행 헬퍼는 **판정용 전문(全文)과 표시용 축약을 분리해 반환**한다
  (`{ out, outForDisplay }`). `slice`/`head`/`truncate` 가 붙은 값이 **비교·포함·파싱에
  쓰이는지** 한 번 묻는다 — 로그용 축약 헬퍼는 어느 repo에나 있고, 그 반환값이 판정에도
  쓰이면 같은 일이 난다.
- ⚠ **게시된 열거값 중 «아무도 못 내는 것» 은 미구현이 아니라 거짓 계약이다** [분류]
  (modfolio-notify 2026-08-05). 계약 열거를 게시하는 repo 는 전부 같은 노출이 있다 —
  이벤트 목록·스코프·상태값.

  notify 실측(하룻밤에 두 건): `rate_limited` 가 게시 SDK 계약에 있고 HTTP **429** 로
  매핑돼 있는데 **내는 코드 0건** · `message.delivered`·`message.read`·`optout.received` 가
  계약에 있는데 **발행 0건**.

  **소비 앱은 그 분기를 짠다.** 그리고 그 코드는 영원히 죽어 있고 **아무것도 실패하지
  않아서 눈치챌 수 없다** — 값이 선언돼 있고, 문법이 맞고, 그 값을 만드는 코드가 없을 뿐이다.
  실제 결과: 상대가 «백오프하라» 는 429 를 **영원히 못 받는다.**

  ⚠ 이건 athsra 의 「주석이 발행하지 않는 이벤트를 광고한다」와 atelier 의 「주석이 읽지
  않는 화면을 단언한다」의 **세 번째 얼굴**이다 — 이번엔 주석이 아니라 **타입 계약**이라
  하류가 그것을 **컴파일러가 보증한 사실**로 받는다. 그래서 셋 중 가장 강하게 믿긴다.

  → **census 를 유한한 방향으로**: 게시된 열거값(유한) → 그 값을 **생산하는** 코드.
  0건이면 위반. 면제는 사유 필수(«상대가 보내는 것을 받기만 한다» 는 정당하다).
- ⚠ **프로브가 «실행되지 않은 채» 결과 모양의 출력을 낼 수 있다 — `rg` 에 경로 인자를
  빠뜨리면 stdin 을 검색한다** (atelier-and-folio 2026-08-05 제보, 허브 양성 대조로 재현).
  앞의 항들이 «도구가 출력을 바꾼다» 라면 이건 **대상이 파일시스템이 아니었던** 경우다.

  `rg PATTERN` 에 경로 인자가 없고 stdin 이 **파이프**면(스크립트를 `python3 - <<'PY'` ·
  `cat x.py | python3 -` 로 돌리는 흔한 형태) rg 는 파일시스템 대신 **stdin** 을 뒤진다.
  허브 재현(`a.txt` 에 needle, 파이프 stdin):

  ```
  rg -l NEEDLE            → exit 1 · 빈 출력   ← «0건» 으로 읽힌다. 파일은 안 봤다
  rg -l NEEDLE .          → exit 0 · a.txt probe.py
  rg -l NEEDLE (DEVNULL)  → exit 0 · a.txt probe.py
  ```

  **두 얼굴이 있고 둘 다 나쁘다**:
  - stdin 이 이미 소진됐으면 → **빈 결과 = «0건»**(허브가 본 형태)
  - stdin 에 스크립트 소스가 남아 있으면 → **그 소스 텍스트를 검색해 «그럴듯한 목록»**
    (atelier 가 본 형태 — 미사용 의존 조사에서 `nuxt`·`vue`·`zod` 20건이 나왔다.
    **`nuxt` 가 미사용일 리 없다**는 비현실성 하나로 살았고, 양성 대조 후 20 → 2 였다)
  - ⚠ stdin 이 **TTY** 면 `rg` 는 **입력을 기다리며 멈춘다**(허브 실측: 2분 타임아웃).
    즉 **터미널에서 손으로 돌리면 재현되지 않는다** — 자동화에서만 나타난다.

  → **처방**: 프로브의 `rg`/`grep` 에 **항상 경로 인자를 준다**. 서브프로세스에는
  `stdin=DEVNULL` 을 준다(우리 프로브는 stdin 을 쓸 일이 없다). 그리고 **양성 대조**를
  먼저 통과시킨다 — 알려진 히트 1건을 못 잡으면 그 측정은 결과가 아니다.
- ⚠ **인용한 문자열이 파일에 실제로 그렇게 있는지 확인한다 — 검색 도구는 출력에 손을 댈 수
  있다** (visualize 2026-08-04 제보, 허브 재현). 앞 항목이 «출력을 자르지 마라» 라면 이건
  «출력이 **원문이 아닐** 수 있다» 다. 자른 것도 아니고 0건도 아니어서 훨씬 안 보인다.

  `rg` 의 **`-r` 은 `--replace`** 다. `-n`·`-i` 같은 무해한 단문자들 사이에 섞여 들어가고
  (`rg -rn` 은 grep 습관으로는 «재귀+행번호» 처럼 읽히지만 rg 는 기본 재귀라 그런 플래그가
  없다), **에러를 내지 않는다** — exit 0, 히트 수도 파일명도 맞고 **인용문만 틀리다.**

  ```
  파일 실제 내용   "ts7:ready": "bun run scripts/ci/ts7-readiness.ts",
  rg -rn "ts7:ready" →   "n": "bun run scripts/ci/ts7-readiness.ts",     ← exit 0
  rg -n  "ts7:ready" → 1:"ts7:ready": "bun run scripts/ci/ts7-readiness.ts",
  ```

  visualize 는 이 출력을 보고 *"허브 `package.json` 의 스크립트 이름이 `n` 으로 깨져 있다 —
  find-replace 사고 같다"* 는 제보를 **실제로 작성했다가** 발신 직전에 잡았다. docstring 도
  로그 문자열도 **세 곳이 일관되게 `n`** 이라 오히려 확신이 올라갔다 — 전부 같은 rg 호출의
  산물이었기 때문이다. **일관성은 독립 증거가 아니다.**

  ⚠ 이건 *"명령 결과를 인용한다"* 는 규율을 **지켜도 안 걸린다.** 규율이 겨냥한 것은
  기억으로 말하는 것인데, 여기서는 도구가 성실히 답하면서 내용을 바꾼다.
  → 파일 내용을 근거로 삼는 주장은 **치환 능력이 없는 도구**로 한 번 더 본다:
  `grep -n` · `sed -n 'Np'` · `git show` · `Read`.

  ##### ★ 규율을 아는 것으로는 안 막힌다 — **`rg -r` 을 아예 쓰지 않는다** (허브 2026-08-17, 한 세션 3회)

  위 항목을 **관리하는 저장소가 같은 세션에서 세 번** 밟았다. 매번 다른 저장소, 매번
  다른 조사, 매번 «자연스러운» 플래그 조합이었다:

  ```
  rg -rn 'visualize-ui' .                    → "name": "n",   (실제: "visualize-ui")
  rg -rn 'apple-mobile-web-app-capable' apps → <meta name="n" …>  (실제: 그 이름 그대로)
  ```

  첫 번째는 **거짓 드리프트 판정** → 형제 repo 에 보낼 피드백 초안까지 작성(발신 전 회수).
  두 번째는 같은 세션에서 **그 사고를 저널에 적은 직후**에 났다.

  «주의하자» 는 처방이 세 번 실패했으므로 규칙을 바꾼다:
  **`rg` 에 `-r` 을 붙이지 않는다.** 행번호는 `-n`, 파일명만은 `-l`, 개수는 `-c` 다.
  `-r` 이 필요한 상황은 조사에 없다(치환은 조사가 아니라 편집이다).

- ⚠ **JSONC 를 정규식으로 주석 제거하면 문자열 안의 `//` 가 잘린다** (허브 2026-08-17).
  `re.sub(r'//.*', '', s)` 는 `"url": "https://…"` 를 `"url": "https:` 로 만든다 → 파싱 실패 →
  `name` 이 **빈 값**으로 나오고, 그것을 「설정에 이름이 없다 = 드리프트」로 오독했다.
  wrangler·tsconfig·biome 설정이 전부 JSONC 라 자주 만난다.
  → 문자열 안/밖을 구분하는 파서를 쓰고 **알려진 정답 여러 건으로 양성 대조**를 먼저 통과시킨다.

- ⚠ **HTTP 프로브는 소비자가 보내는 헤더로 물어야 한다 — 라우팅이 `Accept` 로 갈린다**
  (허브 2026-08-17). 이 문서 §「같은 URL 이 같은 문서를 뜻하지 않는다」는 npm packument 를
  예로 들지만, **평범한 웹 라우팅에서도 똑같이 일어난다**:

  ```
  curl https://app.modfolio.io/                        → 200      ← 기본 Accept: */*
  curl -H 'Accept: text/html' https://app.modfolio.io/ → 301 → https://modfolio.io/
  ```

  구현이 **HTML 내비게이션만** 301 하고 API 호출은 남기도록 의도적으로 짜여 있었다.
  기본 `curl` 로 재고 *"301 이 기록돼 있는데 200 이다 = 미러가 틀렸다"* 로 판정했고,
  하마터면 **없는 결함으로 형제에게 정정 요청**을 보낼 뻔했다. 미러도 구현도 옳았고
  **틀린 것은 측정**이었다.
  → 판별 질문: ***"브라우저가 보내는 것과 내가 보내는 것이 같은가?"*** 다르면 다른 것을 잰 것이다.
- ⚠ **집계는 정확한데 «무엇을 세는지» 가 다를 수 있다** (허브 → athsra 2026-08-04 전달,
  athsra 요청으로 상류 등재). 앞의 항들이 «도구가 출력을 바꾼다/판정이 뒤집힌다» 라면
  이건 **숫자가 전부 맞는데 해석이 틀린** 경우다 — 재현해도 같은 수가 나오므로
  «검산» 으로는 절대 안 걸린다.

  실측: `auth_user_devices` 를 label 로 `GROUP BY` 해서 한 라벨이 **45행**인 것을 보고
  *"같은 기기가 45번 재등록 = churn 이 심하다"* 로 읽었다. 집계는 정확했다.
  그런데 **인접 행을 시간순으로 늘어놓자** 각 행의 `revoked_at` 이 다음 행의 `created_at`
  과 같았다 — 즉 churn 이 아니라 **설계된 토큰 회전**이었다. 45는 «문제의 크기» 가 아니라
  «정상 동작의 횟수» 였다.

  → **`COUNT`/`GROUP BY` 결과로 결론을 내기 전에 그 그룹의 «행 몇 개를 실제로 펼쳐 본다».**
  특히 시계열 테이블에서 «반복» 은 대개 **상태 전이의 흔적**이다. 집계는 전이를 지우고
  개수만 남기므로, 무엇이 반복됐는지가 아니라 **무엇 사이의 전이인지**를 봐야 한다.
- ⚠ **정규식이 «통과 방향»으로 고장 날 수 있다 — `(?!…)` 를 가변 길이 뒤에 놓지 마라**
  (modfolio-notify 2026-08-05 제보, 허브 재현). 앞의 두 항이 «도구가 출력을 바꾼다» 라면
  이건 **판정 자체가 뒤집힌다** — 그리고 뒤집히는 방향이 하필 **초록**이다.

  «대체가 존재하는가» 를 묻는 접근성 검사였다:

  ```js
  /box-shadow|outline\s*:\s*(?!none|0)/.test(css)   // 매치 = «대체가 있다» = 위반 아님
  ```

  허브 재현 — **공백 유무로 갈린다**:

  ```
  outline: none        → 매치      ← 틀렸다. 대체가 없는데 «있다» 로 읽는다
  outline:none         → 매치 안 됨 ← 맞다
  outline: 2px solid   → 매치       (정상)
  box-shadow: 0 0 2px  → 매치       (정상)
  ```

  `\s*` 가 **0개로 되돌아가면** 부정 선행이 **공백 문자 위치**에서 평가되고, 공백은
  `none` 도 `0` 도 아니므로 선행이 성립한다. 즉 엔진이 «성립하는 위치를 찾아» 되돌아가서
  의도하지 않은 자리에서 참을 만든다.

  ⚠ **같은 결함이 입력 형태에 따라 갈리는 것**이 이 부류를 특히 오래 살게 한다 —
  띄어쓰기 없는 코드베이스에서는 정상 동작하므로 «가끔 되는데?» 로 보인다.
  notify 실측: 게이트는 초록이었고 화면에도 문제가 없었다. **음성 대조가 아니었으면
  영영 안 드러났고, 나중에 진짜 `outline:none` 이 들어와도 못 잡았을 것이다.**

  → **정규식 곡예로 «X 가 존재하는가» 를 묻지 않는다.** 구조 단위(CSS 선언 블록 · AST
  노드)로 자른 뒤 판정한다. 부득이 정규식을 쓰면 **부정 선행 앞에 가변 길이를 두지 말고**,
  대조쌍에 **공백 있는 형태와 없는 형태를 둘 다** 넣는다.
- **도구가 원리적으로 볼 수 없는 것은, 그 도구의 통과가 "없다"가 아니라 "안 봤다"이다.**
  스코프를 넓혀도 해결되지 않는 부류가 있다 — 실측: `interactive-semantics`(WCAG 2.1.1)는
  **axe 로 잡을 수 없다.** 정적 HTML 에 클릭 핸들러가 보이지 않기 때문이다(design 2026-07-27).
  a11y 게이트를 axe 하나로만 깐 repo 는 그 floor 가 통째로 비어 있으면서 초록불이다.
  → 게이트를 깔 때 **그 도구가 무엇을 못 보는지**를 함께 적고, 못 보는 자리는 다른 검출기로
  덮거나 "미검사"로 명시한다. 침묵을 커버리지로 세지 않는다.
- **비치명(warn) 경로일수록 이유가 실제로 보이는지 따로 확인한다.** 치명 경로는 멈추니까 눈에 띄지만,
  warn 은 이유가 잘려도 아무도 모른다.
- **로컬이 아니라 소비자가 받는 것을 검사한다** — 게시본은 `files` 로 걸러진다
  (`bun run canary:published`, canon `registry-redundancy.md` 제4조).
- ⚠ **그리고 «같은 URL» 이 «같은 문서» 를 뜻하지 않는다 — 콘텐츠 협상이 갈라 놓는다**
  (허브 2026-08-08 실측). 위 항목이 «어느 파일이 나가나» 라면 이건 **«어느 표현을 받나»** 다.
  같은 주소를 우리와 소비자가 **다른 `Accept` 헤더**로 물으면 서버가 다른 문서를 준다.

  하네스 3.58.0 게시 직후, 같은 packument URL 을 두 헤더로 재니:

  ```
  Accept: application/json                       65 버전 · latest=3.58.0 · has 3.58.0 = true
  Accept: application/vnd.npm.install-v1+json    64 버전 · latest=3.57.0 · has 3.58.0 = false
                                                 ↑ bun/npm 이 실제로 보내는 헤더
  ```

  그 창(**약 7분**) 동안 방금 게시한 버전을 정확히 지정한 `bun add` 가 *"No version
  matching … (but package exists)"* 로 실패했고, 캐럿 범위는 **직전 버전**을 해석했다.
  **게시 로그는 내내 「레지스트리 확인됨」이었다** — 검증이 full 을, 소비자가
  abbreviated 를 봤기 때문이다.

  ⚠ 위 표의 버전 숫자는 **그날의 실측값**이지 현행 주장이 아니다(그래서 본문에서는
  버전을 박지 않는다 — 멤버-대면 표면의 고정 버전은 `version-prose-drift` 가 잡는다.
  이 문단을 처음 쓸 때 실제로 그 게이트에 걸렸고, 그게 옳은 동작이다).

  ⚠ 이 부류는 **로컬 캐시로 오진하기 쉽다.** 콜드 캐시로 재현해도 같은 결과가 나오므로
  «캐시 탓» 이 즉시 반증되고, 그때 남는 그럴듯한 결론이 **「게시가 실패했다」**이다.
  거기서 재게시하면 2026-08-05 사고가 그대로 재현된다(409 no-clobber 인데 타르볼은
  이미 덮어써져 integrity 불일치 → `latest` 가 손상본을 가리킴).

  → **처방**: 게시·배포 후 확인은 **소비자가 보내는 헤더 그대로** 물어라. 그리고 미등재를
  「실패」가 아니라 **「전파 대기」**로 보고하고 창을 준다(허브 `waitForConsumerVisibility`:
  `visible | pending | inconclusive`). 판별 질문 하나: ***"내 확인 요청과 소비자의 요청이
  바이트 단위로 같은가?"*** — URL 만 같고 헤더가 다르면 다른 것을 잰 것이다.
- **정답(베이스라인·스펙·픽스처)이 어디서 왔는지 밝힌다.** 산출물에서 역산했으면 그건 정답이
  아니라 현재 동작의 사본이다. 스크린샷 베이스라인은 **페이지-에러 게이트를 먼저 켜고** 찍는다.
  같은 뿌리의 다른 얼굴: **남의 표면을 미러하는 정합 테스트를 "우리가 적어둔 목록" 에 대조하면
  그건 정합 검사가 아니다** — 우리 객체를 우리 기대에 비추는 것이라 소유자가 메서드를 추가하거나
  이름을 바꿔도 통과한다. 실물을 구성해 **양방향**으로 대조하라(SDK 클라이언트는 대개 네트워크
  없이 구성된다 — 통신은 호출 시점이다). design 실측 2026-07-28: 바꾸자마자 그 게이트가
  즉시 누락 하나를 답했다.
- **canon 을 인용하는 도구는 canon 을 *덜* 집행하는지만이 아니라 *더* 집행하는지도 본다.**
  후자는 **규약 준수를 벌하므로 채택을 직접 막는다.** 실측 2026-07-28(design): 린트 규칙이
  canon 의 열린 패턴(`--space-N` "단계 수·값은 앱 자율", `--shadow-{이름}`)을 **한 앱의 범위로
  열거**해 둬서, canon 이 이름으로 제시한 토큰을 쓴 CSS 가 경고를 받았다 — fleet 규모로
  `--color-danger` 70 파일 · `--z-modal` 60 · `--bp-md` 29 가 전부 미승인 처리였고, 고친 뒤
  connect 경고가 **1131 → 968**(163건 전부 canon 이름)로 떨어졌다. 그 규칙이 **opt-in 이라
  아무도 안 켜서 1년 가까이 안 보였다** — 아무도 켜지 않는 검사는 틀린 채로 오래 산다.
- ⚠ **도달 가능성은 boolean 이 아니라 «어떤 상태에서» 다** (athsra 2026-08-04).
  링크가 **있는데** 그것이 렌더되는 조건이 좁으면, 도달성 검사는 통과하고 기능은 사라진다.
  실측: `/vault/import` 로 가는 링크가 **딱 하나**였고 그 하나가 **빈 상태 블록 안**에
  있었다 — 계정 0개면 보이고 **1개부터 UI 에서 영구히 사라진다.** 그런데 그 화면을 만든
  전제가 *"빈 볼트에 손으로 치게 만들면 한 번 쓰고 떠난다"* 였고, **대부분은 옮길 게 있다는
  걸 한참 뒤에 깨닫는다.** 정확히 그때 문이 없었다.
  판별 질문은 「네 번째 부류」와 같은 형태로 답한다 — *"이 링크를 지우면 무엇이 실패하는가?"*
  → **아무것도.** 다만 바뀌는 것이 안전 성질이 아니라 **발견 가능성**이다.
  → 「링크가 있는가」가 아니라 **「언제 보이는가」**로 묻는다. 라우트가 파일시스템에 있는
  프레임워크면 **정적 라우트 × 그 경로 문자열 언급**의 전수 스윕이 한 화면이다(언급 0 = 문 없음).
  같은 스윕에서 **인바운드 링크 0** 인 배포된 기능도 나온다(athsra: 1117줄·3흐름·라이브,
  주석 한 줄만 그 경로를 알고 있었다). **문이 없으면 없는 기능과 구분되지 않는다.**
- **테스트가 있다는 사실은 그 코드가 도달 가능하다는 증거가 아니다.** "이 함수를 부르는
  **프로덕션** 코드가 있는가" 는 별도 질문이다 — 자기 스펙을 소비자로 세지 않는다.
- ⚠ **«쓰기는 있는데 읽기가 없다» — 코드에 결함이 없고 제품에 구멍이 있는 경우**
  (atelier-and-folio 2026-08-05). 위 항이 **호출자** 축이라면 이건 **소비자** 축이다.

  실측: 랜딩 `/apply` 가 상담 문의를 `anf_leads` 에 저장하는데 **앱 전체에서 그 표를 읽는
  코드가 0건**이었다(`rg -ln "anfLeads" apps/app` → 0). 오너가 문의를 볼 유일한 통로가
  알림 메일이었고 그것도 도메인 검증 전이라 — **아무도 못 읽는 곳에 쌓이고 있었다.**

  ⚠ **타입·린트·테스트·빌드·e2e 가 전부 정상이었다.** 표는 잘 쓰이고 있었고, 쓰는 코드에
  버그가 없었다. 어떤 코드 게이트도 원리적으로 못 본다 — 「검사 대상이 비었다」도
  「정답이 틀렸다」도 아니고, **아무도 게이트를 속이지 않았다.**

  → **census 를 유한한 방향으로 돌린다**(pay 가 세운 축): 코드 → 표는 무한하지만
  **표 → 코드는 유한하다**(선언된 `pgTable` 개수). 선언된 표마다 읽기(`from`/`join`/
  `db.query.X`)와 쓰기(`insert|update|delete`)를 **갈라 세고, 읽기 0이면 위반.**

  ⚠ **엄격하게 하면 정당한 코드를 벌한다** — atelier 실측 3건이 전부 오탐이었고, 그중
  하나는 **더 나은 구현을 벌하는** 형태였다: `insert(...).onConflictDoNothing().returning()`
  이 빈 배열이면 중복이므로 **UNIQUE 제약이 곧 조회**다. select-then-insert 로 «고치면»
  멱등성이 오히려 약해진다. 나머지 둘도 정당했다(대사용 기록은 화면이 `orders` 를 읽는다 ·
  감사 로그는 제안이 API 응답으로 바로 나간다).
  → 면제는 **사유 필수**로 등재한다. 신호는 남는다: **새 표가 write-only 로 들어오면
  실패하고, 사유를 적는 행위가 «정말 괜찮은가» 를 한 번 묻게 한다.** `anf_leads` 에 그
  질문을 했다면 답은 **아니오**였다("오너가 문의를 못 본다").

  ⚠ **같은 스윕이 덤으로 잡는 것 — 스키마 주석이 «없는 소비자» 를 단언한다.**
  ```
  anf_invoices  "status=paid 시 발행 (admin/billing 이 읽음)"  ← 그 참조 0건
  anf_refunds   "order status 를 refunded 로 구동"              ← 참조 0건(전부 상태값)
  ```
  athsra 가 등재한 「스키마 주석은 공개 문서보다 조용하고 더 정확히 복제된다」의 **소비자
  판(版)** 이다 — 그쪽은 주석이 *발행하지 않는 이벤트*를 광고했고, 이쪽은 *읽지 않는
  화면*을 이름 대서 단언했다. 다음 사람은 «billing 이 읽는다니 거기서 찾자» 로 시작한다.
  ⚠ 처방은 **표를 지우는 것이 아니라 주석을 고치는 것**이다(무인 세션이 프로덕션 표를
  드롭하지 않는다 — atelier 의 판단이 옳다).
- **UI 가 단언하는 상태는 읽은 값에서 파생시킨다.** 조건부가 아닌 상태 문자열·숫자는 그 자체가
  결함 후보다 (`rg '>(Verified|활성|완료)<'` 류 스윕).
- **평가셋의 실패 목록을 보고 그 항목을 고치면, 오른 수치는 답을 본 시험의 점수다.**
  위 "산출물에서 역산" 의 한 형태인데 **더 알아채기 어렵다** — 코드가 아니라 *측정 도구*가
  오염되고, 결과는 정직한 개선과 구분되지 않는 숫자로 나온다. 실측 2026-07-28: Muse 검색의
  잔여 실패 29건은 의미 갭이라, 그 목록을 열어본 뒤 번역쌍을 손으로 써 넣으면 `muse:eval` 이
  **반드시** 오른다. → 규칙: 개선안은 **독립 출처**에서 만들고 평가셋으로는 **한 번만** 판정하거나,
  **홀드아웃**을 먼저 떼어 둔다. 실패 목록을 이미 봤다면 그 사실을 적고 손대지 않는 것이 맞다.
- **픽스처가 "앱이 실제로 쓰는 상태"를 재현하지 못하면, 검사 대상이 있어도 그 규칙은 한 번도
  실행되지 않는다.** 빈 대상(0건)과 다르다 — 대상은 112개가 있고 전부 초록인데, **상태가
  현실에 없는 상태**다. 실측 2026-07-28(design): 프리미티브 28종 × 4스택 axe 게이트가 전부
  통과했는데 slot 컴포넌트 20종이 **자식 없는 빈 슬롯**이었다. `<ol>` 비어 있음 → 위반 0 ·
  `<ol>` + 비-`<li>` 자식(= 앱이 실제로 저지르는 실수) → **`list` serious**. 게다가 브라우저가
  잘못된 자식을 조용히 삼킨다(`<table>` 안 `<div>` 는 foster parenting 으로 표 밖으로 끌려나가고,
  `<select>` 의 비-option 은 렌더조차 안 된다) — 그래서 눈으로도 안 보인다. → 컴포넌트·통합
  픽스처는 **props 를 다 채웠다로 부족하고**, slot/children/스코프드 슬롯을 실제 사용 형태로
  채운 케이스가 있어야 한다.
- **결과가 0건이면 "없다"를 의심하기 전에 probe 가 실물을 재현하는지부터 의심한다 — 그리고
  그건 세어서가 아니라 읽어서만 갈린다.** 같은 날 design 이 세 번, 허브가 두 번 밟았다:
  `<(ul|ol)[ >]` 가 **여러 줄 여는 태그**(`<ul\n  id=…`)를 못 봐 5건이 전부 오탐이었고(정정 후
  0건), 허브에서는 ① 게시 타르볼 심볼 계수가 **셸 출력 손상 + `|| echo 0` 폴백**으로 "신규 코드
  0건"을 만들어 게시 사고 경보 직전까지 갔으며(실물은 byte-identical) ② 웹훅 배선 프로브가
  **issuer 호스트를 엔드포인트 호스트로 가정**해 302/404 를 "장애"로 읽을 뻔했다(실제 좌표는
  `login.modfolio.io/sso/*`). → 0건·이상 코드로 결론을 내기 전에 **그 probe 가 알려진 양성
  1건을 실제로 잡는지**(양성 대조)를 먼저 통과시킨다. 통과 못 하면 그 측정은 결과가 아니다.
- **재현 실패는 신고자의 절차를 그대로 밟은 뒤에만 보고한다.** 내 프로브로 바꿔 돌린
  재현 실패는 결과가 아니다. 실측 2026-07-31(athsra, 자기보고): 시크릿 `set` 값 절단
  신고를 받고 **"재현 안 됨"** 으로 기록했는데, 신고서의 개행 케이스(`$'PROBE=abc\ndef'`)
  대신 **CR**(`abc\rdefghij`)로 시험했다. CR 은 실제로 살아남아서 11자가 11자로 나왔다.
  신고자 절차를 그대로 돌렸으면 **첫 시도에 재현**됐고(7자→3자), 파 보니 개행 1종이 아니라
  `trim()`·따옴표 제거·공백값까지 **11개 대표값 중 6개 실패**였다. → 이건 위 "0건이면
  probe 부터 의심한다" 의 **재현 실패 보고** 버전이다. 남의 버그를 못 재현했을 때 가장
  먼저 의심할 것은 버그의 부재가 아니라 **내가 절차를 바꿨는가**다.
- ⚠ **인용은 증거가 아니다 — 그리고 규율을 적용하는 사람이 정확히 그 규율을 안 적용한
  항목에 맹점을 갖는다** (atelier-and-folio 2026-08-05 자기보고, 허브가 웹서치로 반증).

  atelier 가 허브에 *"canon 이 2026-10-15 폐지 예정 라우트를 사문 없이 가르친다"* 를
  제보했다. **방향이 정확히 반대**였다:

  ```
  레거시(폐지)  /accounts/{aid}/workers/namespaces/*        2026-10-15 중단
  대체(현행)    /accounts/{aid}/storage/kv/namespaces/*     ← canon 이 가르치는 것 = 정답
  허브 전수 실측: workers/namespaces  0건
  ```

  경위는 그쪽 자기보고가 정확하다 — 하네스 권고문의 *"KV 레거시 네임스페이스 API"* 라는
  **표현**을 보고 canon 에서 `kv/namespaces` 를 grep 해 나온 것을 폐지 대상으로 단정했다.
  실제 폐지 경로명을 확인하지 않았다. **문자열이 비슷하다는 것을 근거로 썼다.**

  > **★ 그리고 여기가 핵심이다.** 같은 편지에서 그는 *"canon 이 가르치는 것을 실물과
  > 대조하라"* 는 취지의 지적을 **네 개** 했다(레지스트리 5건 중 3건은 프로덕션 DB·DNS·
  > 라이브 HTML 로 직접 쟀다). **그러면서 §4 만 대조를 안 했다.**
  >
  > 규율을 아는 것도, 같은 문서에서 네 번 적용하는 것도 그 다섯 번째를 지켜 주지 않는다.
  > **자기가 인용한 문서는 «검사 대상» 으로 안 보인다** — 그게 근거처럼 느껴지기 때문이다.

  ⚠ 대가가 컸을 자리다. 허브가 그대로 채택했으면 **맞는 경로에 «폐지» 딱지를 달거나 거기서
  떠나는 마이그레이션**을 했을 것이고, 그 canon 은 31 repo 가 읽고 그대로 구현한다.
  멈춘 것은 **웹서치 한 번**이었다.

  → **처방**: 「X 가 폐지/변경/금지됐다」는 제보는 **1차 출처의 정확한 식별자**(경로·플래그·
  버전 문자열)를 대조한 뒤에만 행동한다. «비슷한 이름» 은 근거가 아니다. 그리고 **내가
  남에게 요구한 규율은 내 항목에도 적용됐는지** 발신 전에 한 번 훑는다 —
  네 개를 옳게 하고 하나를 빠뜨리는 것이 이 부류의 전형이다.

  ##### ⚠ **«실물 + 인과» 가 한 노트에 오면 실물만 받고 인과는 따로 잰다** (2026-08-05 허브 자기보고)

  위 항목은 «제보가 틀렸다» 였다. 이건 **제보가 맞았는데 그 옆에 붙은 설명이 추론**인
  경우다 — 그리고 그쪽이 더 위험하다. **실물이 인과에 신뢰를 빌려준다.**

  notify 가 CF 트리거 실물 JSON 을 보냈다(워커 3종 · 최근 5회 빌드 실패 0건 · 배포 Version
  매번 갱신 — **전부 참**). 같은 노트가 *"워크스페이스에서는 `apps/<app>` 경로에서
  `bun install` 이 루트 lockfile 을 못 본다"* 라고 인과를 달았고, 나는 **그 문장을 근거로
  canon 예시를 바꿔 놨다.** 다음 편지에서 notify 가 스스로 철회했다:

  > *"그건 **추론이었지 실측이 아니다.** 나는 `root_directory: apps/<app>` 를 **시도한 적이
  > 없다.** 「캐싱이 안 되고 느려짐」이 「아예 실패」보다 정확한 기술이다 — `bun install` 은
  > lockfile 이 없어도 성공한다. `--frozen-lockfile` 을 쓸 때만 실패한다."*
  > *"§5단계 예시를 고칠 근거로 내 노트를 쓰지 마라. 위 JSON 은 «이렇게도 된다» 의 실물이지
  > **«저건 안 된다» 의 증거가 아니다.**"*

  같은 날 athsra 의 `secrets-policy` 제보에서는 이 규율을 지켰다(직접 체크아웃해서
  `KVNamespace` 0건을 셌다). 차이는 하나다 — athsra 는 «너희 문서가 틀렸다» 였고 notify 는
  **«내 실물은 이렇다»** 였다. 후자에서 검증을 빠뜨리기 쉽다.

  → **처방**: 인바운드에서 «측정값» 과 «왜 그런가» 를 **분리해서 받는다.** 측정값은
  그대로 쓰고, 인과는 우리 쪽에서 재현되기 전까지 **canon 에 쓰지 않는다.** 특히
  «A 가 되니까 B 는 안 된다» 는 **B 를 시도한 적이 있는지** 묻는다 — 대개 없다.
- **선언·목록·문서는 실물과 대조되기 전까지 주장이다.** 앞의 항목들이 *검사*의 실패를
  다뤘다면 이건 그 **앞단**이다 — 아예 검사 대상으로 등록된 적이 없는 사실들. 셋은 서로
  다른 곳에서 같은 방식으로 썩는다. 실측 2026-07-31, modfolio-infra 가 하루에 세 개를
  독립적으로 찾아냈다:
  - **목록**: 리댁터 `SECRET_PATTERNS` 에 athsra device token(`atk_`)만 있고 service
    token(`ats_`)이 없었다. 그런데 그 머신의 `~/.athsra/remote-tokens/` **34개가 전부
    `ats_` 이고 `atk_` 는 0개** — 즉 **안 쓰는 계열만 덮고 실제로 쓰는 유일한 계열은
    못 보고 있었다.** 목록은 원리적으로 "이미 아는 것"만 덮으므로, 목록 옆에는 **모양
    기반 검출**을 같이 둔다.
  - **선언**: `ecosystem.json.harnessLatest`. release-gate 는 `package.json` ==
    `ecosystem.json` == `plugin.json` **세 사본이 서로 같은지**만 봤다 — 셋이 사이좋게
    **존재하지 않는 버전**에 합의할 수 있고, 그동안 fleet 배너는 설치 불가능한 버전을
    매 세션 안내한다(안내대로 해도 아무 일이 없으니 **고칠 수 없는 지시의 영구 반복**).
    사본끼리의 일치는 정합이 아니다. **실물(레지스트리)과 대조**해야 한다.
  - **문서**: canon 이 "자유 phrase 도 된다" 를 설명하며 **그럴듯한 12자 문자열**을 예시로
    들었고, 그게 저널로 번져 게시본 9파일 30곳에 실렸다(공개 npm). 그리고 그 문자열이
    **실제 자격증명으로 쓰이는 지점이 발견됐다** — 문서가 품질 문제가 아니라 **하류 결함을
    생산**했다. 접두사도 엔트로피도 없어 어떤 시크릿 패턴에도 안 걸린다.
  → 셋 다 **대조 상대를 지정하는 것**이 수정이다: 목록↔실사용 · 선언↔레지스트리 ·
  문서↔자리표시자 규칙. 그리고 그렇게 만든 검사는 **실물로 대조쌍을 세운다** — 이 문서
  게이트는 합성 픽스처였으면 통과했을 자리에서 창시 사례를 놓쳤고(문맥이 줄이 아니라
  **감싸는 제목**에 있었다), `git show` 로 꺼낸 수정 전 실물로 잡았을 때만 걸렸다.

#### **«이미 대응했다» 는 «지금도 대응돼 있다» 를 보장하지 않는다** (2026-08-04 connect → 허브 실측)

위 항목들은 전부 «선언이 실물과 다르다» 였다. 이건 **선언이 옳았던 적이 있다** — 그리고
아무도 손대지 않았는데 거짓이 됐다. 대조 상대가 **바깥에서 움직이기 때문**이다.

connect 실측: 이전 CVE 때문에 `brace-expansion` 을 `^2.1.2 || ^5.0.8` 로 이미 핀해 뒀는데,
**mitigation 을 우회하는 후속 advisory**(GHSA-rgw5-rvv9-x895)의 범위가 `>=4.0.0 <5.0.9` 라
**그 핀 자체가 범위 안**이었다. 기록은 «대응 완료» 라고 말하고 있었다.

허브가 이 노트를 등재하려고 자기 저장소를 열었더니 **같은 것이 그 순간 진행 중이었다**:

```
40분 전  quality:all  exit 0            ← check:audit 포함, 취약 0
지금     check:audit  exit 1  (high 2 · moderate 1)

fast-uri  >=4.0.0 <4.1.2   ← 우리 overrides 는 "^4.1.1" (이전 advisory 대응)
undici    >=7.0.0 <7.29.0  ← 우리 overrides 는 "^7.28.0" (이전 advisory 대응)
```

**코드도 lockfile 도 안 건드렸는데 빨개졌다.** 두 핀 다 «대응했다» 의 산물이었고, 새
advisory 범위가 **정확히 그 대응값을 포함**했다. 4.1.2 · 7.29.0 으로 올려 0 으로 닫았다.

**왜 이 형태가 조용히 유지되는가 — 「핀은 바닥을 정하지 천장을 밀지 않는다」** (athsra 2026-08-04).
대응으로 `^3.1.3` 을 적어 두면 **위로는 열려 있지만 lockfile 은 이미 3.1.4 에 고정**돼 있다.
아무도 `bun update` 를 돌리지 않으면 **영원히 3.1.4** 이고, 그 사이 새 advisory 범위가
`>=3.0.0 <3.1.5` 로 올라오면 그 핀은 «대응» 이 아니라 **취약본을 고정하는 장치**가 된다.
캐럿은 의도를 적은 것이지 값을 민 것이 아니다.

> **가장 아픈 형태는 이전 advisory 대응으로 넣은 핀이 다음 advisory 를 잡아 두는 것이다.**
> `^3.1.3` 은 «3.1.3 이상» 이라 안전해 보이지만, 락이 3.1.4 에 있으면 3.1.5 로 갈 이유가
> 아무 데도 없다. **핀을 넣은 사람은 자기가 보호막을 쳤다고 기억하고, 그게 감옥이 된다.**
> (athsra 2026-08-04)

→ 요구:
- **예외·핀·mitigation 기록은 «누가 언제 왜» 만이 아니라 «어떤 범위에 대해» 를 적는다.**
  범위가 없으면 후속 advisory 와 대조할 수가 없고, 그러면 재검토는 사람의 기억에 달린다.
- **감사 게이트는 «지난번에 0이었다» 를 근거로 건너뛰지 않는다.** 이 축의 대조 상대는
  저장소 밖(advisory DB)에 있고 **우리 커밋과 무관하게 움직인다** — 위 「배포 대상이 저장소
  밖에 있다」(sign)와 같은 형태이며, 그래서 게이트가 **매번** 실물을 물어야 한다.
- `revisit_after` 날짜는 **하한**이지 판정이 아니다. 날짜가 안 왔어도 범위가 겹치면 그 예외는
  이미 무효다.

### 왜 이 절이 규칙인가 — 하루에 다섯 repo 가 독립적으로 밟았다

2026-07-27, 서로 조율하지 않은 다섯 저장소가 **같은 클래스**를 각자 발견했다:

| repo | 발현 |
|---|---|
| modfolio-design | axe 스코프 · 스캔 0건 · 관대한 스텁 · 픽스처 타입(4스택이 똑같이 틀림) |
| modfolio-connect | 카탈로그 census 가 상수 참조만 세어 **리터럴 발행 16종을 통째로 놓침** · 스펙을 소비자로 셈 · 구현에서 역산한 스펙이 fail-open 을 명세화 |
| atelier-and-folio | *"유도 목록이 비면 검사가 0개 돌면서 초록불로 통과한다"* · 테스트 스크립트가 디렉터리로 좁혀져 **파일이 실행조차 안 됨** |
| modfolio-pay | 베이스라인이 **에러 화면**을 정답으로 굳힘 · 게이트 트리거에 그 게이트 자신이 빠짐 · 마스크 0매칭 무력화 · a11y 22/22 초록인데 런타임이 던지고 있었음 |
| modfolio-ecosystem | `grep "N pass"` 로 게이트 판정 · warn 의 이유 절단 · `Glob` dot-제외로 새 게이트가 아무것도 안 봄 · **`bun audit` 을 손으로 0 만들고 게이트는 미배선** | [역사]

**다섯 번이면 우연이 아니라 구조다.** 도구가 다르고 언어가 다르고 도메인이 달라도 형태가 같다 —
**검사 대상이 비어 있거나 좁거나 가짜인데, 결과는 통과로 보고된다.** 그래서 이건 특정 도구의
함정이 아니라 **초록불을 읽는 방법**의 문제이고, 규칙이어야 한다.

### 네 번째 부류 — **뒤집어도 아무것도 안 깨지는 불변식** (2026-08-03 modfolio-pay)

앞 세 부류는 전부 *"검사가 무언가를 잘못 본다"* 였다. 이건 **볼 것이 애초에 없다.**

실측: pay 에 사업자가 둘이 됐고(모드폴리오 + 에이앤에프), 전금법상 크레딧이
**발행인 외의 제3자에게서 쓰이면** 자기발행형 선불이 아니라 선불전자지급수단이 되어
**이미 판매된 크레딧 전부에 소급으로** 선불업 등록이 요구된다. 그래서 코드에 필드를 뒀다:

```ts
readonly acceptsCredits: boolean;   // 발행인만 true
```

**이 플래그를 뒤집으면 아무 기능도, 화면도, 다른 테스트도 안 깨진다. 제품이 법적으로
무엇인지만 바뀐다.** 타입도 린트도 게이트도 원리적으로 볼 것이 없다 — 그래서 *"한 명만
true"* 를 단언하는 **테스트가 유일한 방어**다.

→ **규칙**: 값을 바꿔도 동작이 그대로인데 **법적·계약적·안전상 성질만 바뀌는 상수·
플래그·목록**은 반드시 단언한다. 판별 질문 한 줄: *"이걸 잘못 설정하면 무엇이
실패하는가?"* — 답이 **"아무것도"** 면 그 자리가 테스트가 필요한 자리다. 주석은 방어가
아니다(주석은 실행되지 않는다).

같은 형태가 이 universe 에 더 있다: 소비 좌표 단일 원칙(`registry-redundancy`) ·
consent 게이트(`knowledge-sovereignty`) · `minTier`(로컬 레일에 넘기면 안 되는 task
class) — 전부 틀려도 초록이 나온다.

### 발신 경계의 스키마는 **생산자보다 엄격하면 안 된다** (2026-08-03 허브 자기정정)

공유 이벤트 스키마를 등재하면서 필드 **이름**은 생산자 원문에서 가져왔는데 **제약은
지어냈다** — `refund_amount: z.number().positive()` · `currency: z.enum([...])`.
생산자 실물은 `z.number()` · `z.string()` 이었다.

이게 왜 위험한가: 그 생산자의 `publishEvent` 는 `safeParse` 실패 시 이벤트를
**DROP** 한다(아웃박스에 도달조차 못 한다). 더 엄격한 공유 스키마를 채택하는 순간
**구조적으로 멀쩡한 정산 이벤트가 조용히 사라진다.** 생산자가 금액을 `z.number()` 로
두는 것은 느슨해서가 아니라 **불변식을 원장 소스에서 강제하고 이벤트 경계에서 재단언
하지 않기 때문**이다 — 재단언이 곧 drop 위험이다.

그리고 그 발명 위에 *"0원 환불은 이벤트가 아니다"* · *"없는 통화는 거부"* 라는 테스트를
붙여 **발명을 정답으로 굳혔다**(위 「두 번째 부류」 그대로, 이번엔 허브 자신이).

→ **원칙**: 공유 스키마는 **생산자가 실제로 내보내는 것**을 기술한다. 그보다 좁으면
계약이 아니라 희망이고, 그 희망이 데이터를 삼킨다. 더 강한 보장이 필요한 소비자는
**자기 쪽에서 좁힌다**(수신 엄격 · 발신 관대 — 방향이 반대면 안 된다).
⚠ 관대함이 "아무거나 통과" 는 아니다. 타입·구조는 여전히 거부한다.
⚠ enum 을 발신 경계에 박으면 값 하나 추가가 published 패키지의 breaking bump 가 되고,
그 사이 그 값의 트래픽은 전부 유실된다.

### census 는 **방향을 뒤집으면 유한해진다** (2026-08-03 modfolio-pay)

*"계약에 없는데 발행되는 이벤트"* 를 잡으려고 **생산자 코드를 세면** 리터럴·동적 조립·
조건부 발행을 계속 놓친다(pay 2026-07-27 실측: 상수 참조만 세어 **리터럴 발행 16종을
통째로 놓침**). pay 를 구한 것은 census 를 더 똑똑하게 만든 게 아니라 **방향을 뒤집은
것**이다:

| 방향 | 성질 |
|---|---|
| 생산자 코드 → 계약 | 대상이 **무한**하다(코드의 모든 발행 지점). 놓치기 쉽고 오탐도 많다 |
| **계약 유니온 → 발행 코드** | 대상이 **유한**하고 목록이 이미 있다. **0건이 곧 답이다** |

역방향은 *"안 쓰는 계약"* 만 잡고 *"계약에 없는 발행"* 은 못 잡으니 **둘 다** 필요하다.
다만 **유한한 쪽을 먼저 만든다** — 목록이 유한해야 실물 대조가 가능하다.

### 다섯 번째 부류 — **검사 표면이 «배포 대상»을 포함하지 않는다** (2026-08-03 modfolio-sign)

앞의 넷은 전부 *검사가 무언가를 잘못 본다* 였다. 이건 **검사가 옳고, 대상이 저장소 밖에 있다.**

실측: 대시보드를 배포했다. `release:gate`(test·typecheck·build·lint·e2e) **전부 초록**,
`wrangler deploy` **성공**, 라이브 **200**. 그런데 프로덕션 D1 에 마이그레이션이 적용되지
않은 채였고, `/app/+layout.server.ts` 가 `membershipsOf()` 를 부르는 **레이아웃**이라
**로그인한 사용자는 모든 화면에서 500** 을 받았다. *"초대받은 앱이 없습니다"* 화면은
**도달 불가능**이었다.

**왜 아무 게이트도 못 봤나 — 구조적이다.** 타입검사·린트·테스트·빌드는 전부 저장소 안을
본다. 로컬 D1 은 테스트가 매번 스키마를 만들어 주므로 **로컬은 언제나 최신**이다.
프로덕션 스키마는 저장소 밖에 있고, 파이프라인 어디에도 *"프로덕션이 이 코드를 감당하는가"*
를 묻는 단계가 없었다.

> 그 repo 의 인계 문서는 상태를 *"`ms_app_members` **0행**"* 이라고 적고 있었다.
> 실측은 **`no such table`** 이었다. **자기 자신에 대한 진술이 틀렸다** — 「선언은 실물과
> 대조되기 전까지 주장」이 자기 repo 안에서도 성립한다.

**요구**:
- **배포 전** 원격 마이그레이션 원장(`d1_migrations` 등) ↔ 저장소 마이그레이션을 **양방향**
  대조한다. `로컬 ⊄ 원격`(미적용)도, **`원격 ⊄ 로컬`(저장소가 모르는 것이 프로덕션에 있다 —
  더 위험하다)**도 실패다.
- ⚠ **`wrangler d1 migrations list --remote` 를 파싱하지 마라.** 미적용이 있어도 **exit 0**
  이고(sign 실측) ASCII 표는 계약이 아니다. **테이블을 조회**해서 판정한다.
- 이 게이트는 **`release:gate` 가 아니라 배포 명령 앞**에 건다. pre-push 훅이 쓰는 게이트는
  오프라인·무인증에서 통과해야 하기 때문이다. **그 구분이 중요하다.**
- 짝이 되는 **오프라인 축**을 같이 둔다: 코드가 선언한 테이블(`sqliteTable` 등) ↔ 마이그레이션
  **재생** 결과. ⚠ 재생은 **파일 순서대로** — 문장 종류별로 묶으면(CREATE 전부 → RENAME 전부)
  SQLite 테이블 재작성 관용구에서 **테이블이 통째로 증발**한다(sign 실측 5개).
- **런타임 fail-loud 도 유효한 형태다.** 허브 `apps/loom` 은 매 요청 앞에서 기대 테이블 존재를
  확인하고 없으면 **503 + 없는 테이블 이름**을 준다(`no such table` 을 500 으로 흘리지 않는다).
  배포 전 게이트를 대체하진 않지만, 갭이 열렸을 때 **증상을 진단 가능하게** 만든다.

**같은 날 두 번째 얼굴 — 관측 가능한 표면 ≠ 시스템의 표면.** 배포 후 스모크 10건이 전부
통과하는데 로그인 토큰 교환이 401 이었다. 무인증 표면은 완벽히 건강하고 **서버 대 서버 구간**이
깨져 있었다. **브라우저 없이 지나갈 수 없는 구간은 라우트 프로브로 안 보인다** — 그런 구간에는
자격증명 없이 도는 별도 프로브를 찾아 둔다(sign 은 `POST /sso/device_authorization` 이 클라이언트
등록 상태를 세 값으로 구분해 준다는 것을 찾았다).

### **optional + 기본값은 분류를 강제하지 않는다 — 숨긴다** (2026-08-04 modfolio-notify → pay 실측)

어떤 축으로 분류를 강제하고 싶으면 그 필드를 **필수·무기본값**으로 둔다. optional 이면
미분류 항목이 안 걸리고, **기본값이 있으면 미분류가 조용히 한 칸에 몰린다** — 그리고 그 칸이
늘어나는 것을 아무도 안 본다.

실측: pay 가 `EmailKind` 를 **기본값 없는 필수**로 넣자 타입체커가 즉시 갈랐다.

```
배럴이 export 하는 템플릿      12
실제 sendEmail 호출 지점       13   ← weekly-digest.ts (배럴을 안 거친다)
```

*"우리 메일은 12종"* 이라는 **자기 인식이 틀렸다**는 것이 드러났다. pay 자신의 문장:

> *"이 발견은 `kind` 를 optional 로 뒀으면 안 나왔다. 기본값이 있었으면 13번째가 조용히
> 그 값을 물려받고 목록은 계속 12로 보였을 것이다."*

**부수효과가 본래 목적보다 먼저 온다**: 분류를 붙이려던 것인데 **컴파일러가 그 축의 전수
목록을 만들어 준다.** 「선언·목록은 실물과 대조되기 전까지 주장」의 예방형이다 — 대조할
필요가 없게, 타입이 목록을 강제로 완성시킨다.

후보 축(fleet 에 같은 형태가 여럿): 이벤트 종류 · 로그 레벨 · 권한 스코프 · 과금 항목 ·
알림 채널. **판별 질문**: *"이 필드를 optional 로 두면, 분류 안 된 것이 몇 개인지 누가 세나?"*
답이 «아무도» 면 필수·무기본값이 맞는 자리다.

### 짧은 것들 — 같은 축, 각각 실측 1건 (2026-08-03 fleet 라운드)

- **rule 이 처방하는 명령을 멤버가 못 돌 수 있다** (athsra). 이 문서가 `bun run json-diff` 를
  MUST 로 주는데 스크립트는 tarball 에 실려 가고 **배선은 안 갔다** — 멤버에선
  `Script not found` 였다. 그리고 위 「종료코드」 함정과 겹쳐 **조용히** 실패해, 빈 출력이
  "키 델타 없음" 으로 읽힐 뻔했다. → 허브가 `MEMBER_WIRED_SCRIPTS` 로 배선하고
  `verify:rule-commands` 가 대조한다. **rule 은 자기 실행 가능성을 스스로 보증해야 한다.**
- **문서가 canon 이 금지한 것을 가르칠 수 있다** (athsra). 허브의 내부 온보딩 가이드가
  canon 이 이름을 대며 금지한 엔드포인트 직행 + sessionStorage PKCE 를 처방하고 있었다.
  타입도 테스트도 안 보는 표면이라 아무것도 빨개지지 않았고, **새로 붙는 앱이 그대로 구현**했다.
  → canon 이 이름으로 금지한 것이 문서의 **코드 블록**에 나오면 실패하는 검사가 가능하다.
- **`git ls-files` 는 새 파일을 게이트에서 숨긴다** (atelier). 아직 추적되지 않은 파일은
  목록에 없으므로, 그 목록으로 도는 검사는 **방금 추가한 파일을 안 본다** — 저장소 사본을
  전수 스캐너가 *더* 보는 것(위 `.gitignore` 항목)의 **반대 얼굴**이다. 둘 다 "검사 표면이
  의도한 범위와 다르다" 의 사례다.
- **게이트 순서: 테스트가 빌드보다 먼저 돌면서 빌드 산출물을 읽을 수 있다** (sign).
  그러면 첫 실행은 stale 산출물을(또는 없어서) 보고, CI 는 순서를 바꾸면 갑자기 빨개진다.
  산출물을 읽는 검사는 **그 산출물을 만드는 단계 뒤에** 온다는 것을 순서로 못 박는다.
- **실패가 한 축에 몰렸는지 먼저 본다** (pay). *"실패가 mobile 에만 몰렸는가"* — 몰려 있으면
  개별 케이스가 아니라 **그 축의 설정**(뷰포트·로케일·타임존·권한)이 원인일 가능성이 높다.
  개별 실패를 하나씩 고치기 전에 축을 의심하면 N건이 1건이 된다.
- **`svelte-check` 0 errors 인데 화면에 `[object Object]`** (athsra). 타입 검사가 통과해도
  **렌더 결과의 문자열화**는 아무도 안 본다. 객체를 텍스트 슬롯에 넣는 것은 타입상 합법이다.
  → 렌더 산출물에서 `[object Object]` · `undefined` · `NaN` 을 찾는 스윕을 따로 둔다.

### 다섯 번째 부류의 새 얼굴 — **앱이 어댑터의 제거를 자기 선언으로 가린다** (2026-09-06 dle-desk · 허브 Writ lane)

Astro 5→7 + `@astrojs/cloudflare` 12→14 상향. **check 0 · lint 0 · test 479/479 · build 0 · `wrangler deploy --dry-run` 0** —
다섯 게이트가 초록이었고 프로덕션 `/dashboard`·`/api/*` 는 500 이었다. `/login` 은 200 이라 «살아 있다» 로 보였다.

로컬 `astro preview`(workerd) 재현 원문:

```
[ERROR] Error: Astro.locals.runtime.env has been removed in Astro v6. Use 'import { env } from "cloudflare:workers"' instead.
    at get env (file:///…/dist/server/entry.mjs:55:11)
```

어댑터 13+ 는 `locals.runtime` 을 **제거**하고 접근에 throw 하는 getter 를 남겼다. 그런데 이 앱의 `src/env.d.ts` 가
`App.Locals.runtime` 을 **스스로 선언**해 두었으므로 `astro check` 는 통과했고, 빌드는 실행이 아니라 번들이고, vitest 는
어댑터 런타임을 타지 않는다. 검사 표면(타입·번들·단위 테스트)이 **배포 대상(workerd 런타임)을 포함하지 않았다** —
다섯 번째 부류 그대로다. 다른 점은 **가린 쪽이 우리 자신**이라는 것: 앱의 선언이 라이브러리의 제거를 덮었다.

- ***"이 조건을 만족시키는 가장 싼 방법이 무엇인가?"*** — 타입 검사를 통과시키는 가장 싼 것은 «우리가 선언한 타입» 이고,
  그것은 라이브러리가 무엇을 실제로 내놓는지와 무관하다. **자체 `declare global` 로 라이브러리 타입을 덮는 곳은 전부**
  런타임 프로브가 필요한 자리다.
- 검출기는 하나뿐이었다: `astro preview` + `curl /api/health`. 그것을 **배포 뒤에** 돌렸다. 같은 날 muje 는 배포 전에
  돌렸고(`/` · `/login` · `/api/health` 200) 같은 클래스를 사전에 잡았다.
- 부수 사건: `athsra run … -- wrangler deploy --env staging` 이 **프로덕션 워커**로 갔다(출력의 워커 이름·최상위 var 로
  확인). `--dry-run` 을 돌리고도 **exit 0 만 읽고 `Uploaded <name>` 을 읽지 않았다** — 「초록불 = 검사됐다」 의 오독이
  운영 축에서도 반복됐다.
- 처방: `predeploy` 런타임 프로브 · dry-run 이름 대조 · Writ 는 배포 위임이 아니다(`orbit.md`). 사건 기록
  `docs/incidents/2026-09-06-dle-desk-astro7-prod-deploy-500.md`.
