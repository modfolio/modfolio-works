---
title: 게이트를 만들고 검증하는 법
tier: reference
applicability: always
sync_to_siblings: true
version: 1.0.0
last_updated: 2026-08-23
---

# 증례 — 게이트를 만들고 검증하는 법

대조쌍 의무의 세 층, 게이트가 선언한 전제, 실행 형태·환경 상수(TZ), 관찰 창, 그리고 업그레이드가 남의 게이트를 깨는 경우.

> **이 파일은 규범이 아니라 증거다.** 규범은 `.claude/rules/agent-evidence.md` 에 있고,
> 그 규칙의 각 항목이 여기 사례를 가리킨다. 2026-08-23 에 `agent-evidence.md` 가
> **137KB** 로 자라 매 턴 30+ repo 에 주입되고 있었기에 **원문 그대로** 분리했다 —
> 요약하면 측정값이 죽는다. 규범은 주입되고, 증거는 필요할 때 읽는다.

---

## 의존성을 올리면 **그 repo 의 게이트가 깨진다** — 업그레이드는 초록까지가 완료다 (2026-08-08 허브)

evergreen 원칙은 *"drift 는 여는 순간 해소되는 transient"* 라고 말한다. 실측은 다르다 —
**여는 순간 빨개진다.** `modfolio-design` 을 하네스 3.44.2 → 3.60.4 로 올리자 push 게이트가
**네 단계 연쇄**로 막혔고, 하나를 고치면 다음이 나왔다(게이트가 첫 실패에서 멈추므로
한 번에 하나씩만 보인다):

| 깨진 것 | 왜 |
|---|---|
| 포매터 | `harness-pull` 산출물이 그 repo 의 biome 설정과 다른 형태 |
| `brand/registry.json` ↔ 허브 미러 | 게이트가 **`node_modules/@modfolio/harness/ecosystem.json`** 을 정답으로 읽는다 — 하네스가 그 미러를 함께 나른다 |
| sibling 토큰 스냅샷 | 형제 repo 가 자기 토큰을 바꿨고 스냅샷이 stale |
| README 버전 주장 | `doc-version-drift` 가 3.44.2 주장 vs 실측 3.60.4 를 잡았다 |

⚠ **두 번째 줄이 핵심이다.** 멤버가 «허브의 사실» 을 자기 게이트의 정답으로 삼으면,
**허브가 사실을 갱신하는 순간 그 멤버의 push 가 막힌다.** 허브는 자기가 누구를 막고
있는지 모르고, 멤버는 자기가 안 바꾼 것 때문에 빨개진다. Hub-not-enforcer 를 선언해
놓고 **사실상 집행**하는 형태다 — 강제하지 않겠다는 것이 강제가 된다.

⚠ 그리고 세 번째 줄은 이 워크스테이션 특유다: **형제 세션이 동시에 도는 동안 sibling
실물이 계속 움직인다.** 스냅샷 대조 게이트는 그 움직임을 매번 빨강으로 번역한다.

→ **요구**:
- **업그레이드의 완료 정의는 «버전이 올랐다» 가 아니라 «그 repo 의 게이트가 초록이다» 다.**
  올려 놓고 커밋만 하면 다음 사람이 남의 빨강을 물려받는다.
- 게이트가 하나씩만 드러나므로, push 를 반복해 시행착오하지 말고 **`quality:all` 을 먼저
  직접 돌려 전부 본다**(허브는 이번에 push 를 4번 실패하고서야 그렇게 했다).
- 미러를 정답으로 읽는 게이트는 **무엇의 미러인지**를 실패 메시지에 적는다. *"허브
  ecosystem.json 과 어긋난다"* 만으로는 fact-ownership 상 **어느 쪽이 틀렸는지** 알 수 없다
  (이번엔 멤버 스냅샷이 stale 이었고 실측[261 커밋·6분 전]이 허브 쪽을 지지했다).

### 새 게이트를 만들 때 (대조쌍 의무 — 결함→실패 · 정상→통과)

**통과하는 것만 확인하면 teeth 가 없는 게이트다.** 반드시 **되돌려서 실패하는지** 본다 —
결함을 일부러 심고 그 게이트가 잡는지. 이 저장소는 이걸 반복해서 배웠다(G31 ④ 의 버전 표류
게이트는 처음에 **아무것도 검사하지 않은 채** 초록이었다 — `Glob` 이 dot-디렉터리를 기본
제외해 `.claude/**` 전체가 비가시였고, 음성 대조가 아니었으면 모르고 넘어갔다).

**그런데 그 한 방향만으로는 계측 오류가 통과한다.** 대상이 통째로 비어 있거나 입력이 안
읽히는 게이트에 결함을 심어도 여전히 "잡힌다" — 결함이 있으면 실패하는 건 맞지만, 정상
입력에서도 실패하고 있기 때문이다. 그래서 **쌍**으로 확인한다:

- 알려진 **결함** 입력 → 게이트가 **실패**해야 한다
- 알려진 **정상** 입력 → 게이트가 **통과**해야 한다

**⚠ 그리고 그 전에: 결함이 «정말 심겼는지»를 먼저 단언한다** (atelier 2026-08-03).
주입에 실패해도 결과는 **똑같이 exit 0** 이라, 그러면 결론이 뒤집힌다 — 멀쩡한 가드를
*"teeth 가 없다"* 로 읽고 뜯어고치게 된다. atelier 실측: 음성 대조 3개 중 2개가 exit 0 이라
가드를 의심했는데, 실제로는 앵커가 **탭 4개 vs 3개**로 안 맞았고(치환 0건) 다른 하나는
`replace(old,new,1)` 이 128행 코드가 아니라 **21행 주석**의 같은 문자열을 바꿨다. 파일이
안 바뀐 것이었고, 앵커를 고치니 전부 exit 1 이 났다.

```sh
python3 -c "
s=open(F).read(); assert ANCHOR in s, '앵커 없음'          # ① 앵커가 실재하는가
assert s.count(ANCHOR)==1, '앵커가 유일하지 않다'           # ② 엉뚱한 데를 바꾸지 않는가
open(F,'w').write(s.replace(ANCHOR, BROKEN, 1))
print('주입 landed:', BROKEN in open(F).read())            # ③ 실제로 바뀌었는가
"
bun run <gate>; echo exit=$?      # ← "landed: True" 를 본 뒤에만 이 값을 읽는다
```

`False` 면 그건 **가드 결과가 아니라 대조 실패**다. 이건 「0건이면 «없다»를 의심하기 전에
probe 가 실물을 재현하는지부터 의심한다」의 **결함 주입** 버전이다 — 대상이 probe 가 아니라
내 조작이다.

⚠ **러너 초록 ≠ 타입 초록.** 같은 제보 #3: `bun test` 는 타입을 안 본다(`brand-tokens.test.ts`
가 8/8 통과인데 `tsc` 는 TS2769). 허브도 이번에 같은 것을 봤다 — MCP 도구 이름을 망가뜨린
상태에서 테스트는 exit 1, `typecheck` 는 **0**. **두 게이트를 따로 읽는다.**

실측 2026-07-29(connect): 신설 CSS 게이트의 첫 실행이 "shortcut 158개 중 0 emit" 을 보고했고,
원인은 대상이 아니라 `--config` 의 기준계 불일치였다(config 경로는 repo 루트 기준인데 cwd 는
앱 디렉터리). 결함을 심는 방향으로는 안 걸린다 — **빈 스타일시트에 결함을 심어도 잡히니까.**
걸린 건 오직 두 번째 방향(문제 힌트를 *제거*했더니 exit 0 이 나오는가)이었다.

**그리고 이 축은 초록에만 있는 게 아니다.** 결과가 비현실적이면 — 0건이든 **100% 위반**이든 —
"대상"보다 **계측**을 먼저 의심한다. 빨강 100% 는 0건과 같은 지문이다. 같은 날 허브도 두 번
밟았다: zsh 가 `$ref:apps/...` 를 파라미터 modifier 로 파싱해 9/9 프로브가 `0B` 를 냈고,
`bun -e` 의 파이프 stdin 읽기가 **모든** 입력에 빈 문자열을 반환했다. 둘 다
`git show … | wc -c` = 51983 이라는 **양성 대조를 세운 뒤에야** 갈렸다.

한 방향만 확인한 게이트는 검출력이 **아직 미측정**이다.

⚠ **«비어 있다» 가 결함인지 판정하려면 그 자리가 무엇인지부터 봐야 한다 — override 층의
부재는 갭이 아니다** (허브 2026-08-04 자기정정). 새 게이트가 *"자기 platform-adapter 에
Connect 소비자로 선언했는데 `ecosystem.json.oauthClients` 에 없다"* 를 **미러 갭 9건**으로
보고했고, 나는 그걸 형제에게 «허브 기록이 빠졌다» 로 통보했다. **틀렸다.**
`oauthClients` 는 인구조사가 아니라 **override 층**이다(`oc.clientId ?? raw.oauthClientId`) —
파생이 이미 맞으면 **항목이 없는 것이 정상**이고, 9건 전부 파생 clientId 가 선언과 같았다.

그리고 «채우는» 행위가 실제로 위험했다: 관례대로 `redirectUris: []` 를 같이 넣었으면
`connect-drift` 의 `if (e.redirectUris.length === 0) continue` 에 걸려 **그 9 repo 가 드리프트
검사에서 통째로 빠진다.** 기록을 보강하려던 행위가 **검사를 끄는 회귀**가 된다.

→ 판정을 **파생 결과와 대조**로 바꿨다(선언 clientId ≠ 파생 clientId 일 때만 «미러 필요»).
대조쌍: 파생을 일부러 어긋내면 `미러 필요 1` 로 정확히 지목, 되돌리면 0.
**교훈: «X 에 항목이 없다» 를 결함으로 부르기 전에 X 가 «필수 목록» 인지 «예외 목록» 인지 본다.**

⚠ **주입을 되돌릴 때 `git checkout <file>` 을 쓰지 않는다** (2026-08-04 허브 실측, 자기보고).
그 파일의 **미커밋 작업까지 같이 지운다.** 이 저장소는 새 프로브를 구현하는 중에 그 프로브
파일에 결함을 심었다가 `git checkout` 으로 복구했고, **방금 쓴 구현 전체가 HEAD 로 되돌아갔다**
(다행히 컨텍스트에 남아 재적용했다). 대조쌍은 «작업 중인 코드» 에 심는 일이 많으므로 이건
드문 사고가 아니다. → **주입 전에 커밋**하거나, 사본(`cp`)을 떠 두고 사본에서 복구한다.
같은 축의 이웃 함정: 형제 세션의 `git add -A` 가 이쪽 WIP 을 자기 커밋에 쓸어 담는다.

#### 세 번째 층 — **가드가 무는 것이 «옳은 것»인가** (2026-08-04 atelier-and-folio)

①(결함에 실패하는가)과 ②(주입이 landed 했는가)를 **둘 다 통과하고도**, 가드가
**자기가 만들어진 이유인 그 사건 위에서 초록**일 수 있다.

atelier 실측. 원 결함: `/api/folio/notifications` 가 `isNotNull(dueAt)` 로 걸렀는데
그 컬럼에 **프로덕션 쓰기가 0건**이었다 — **만들 수 있는 모든 과제가 학생 알림에서
잘려 나갔다.** 만든 가드: *"`isNotNull(<컬럼>)` 로 거르는데 그 컬럼에 쓰는 코드가
없으면 실패."* 대조쌍 **세 방향 전부 통과**했다(결함 심음 → exit 1, 제거 → exit 0,
변형 → exit 0). 거기서 멈췄으면 «가드 완성» 이었다.

그리고 한 질문을 더 했다 — **«이 가드에 어제의 그 사건을 그대로 먹이면?»**
**못 잡았다.** 다른 파일에 `dueAt: dueAtDate` 쓰기가 하나 있어서 *"쓰는 코드가 없다"* 가
거짓이 됐고, 가드는 «쓰기 존재»만 보고 «그 경로가 실제로 도달하는가»는 안 봤다.

**요구**: 새 가드를 만들었으면 **그것을 만들게 한 사건을 그대로 재현해 먹인다.**
합성 결함을 잡는 것과 **원본 사건**을 잡는 것은 다른 명제다 — 합성은 내가 가드를 보고
만들었으므로 통과가 보장돼 있다(「평가셋의 실패 목록을 보고 그 항목을 고치면」의 가드 버전).

판별 질문: *"이 가드가 어제 있었다면 어제의 그 사건을 막았겠는가?"* 답이 «모르겠다» 면
아직 미측정이고, «아니오» 면 가드가 **다른 것을 물고 있다.**

##### ⚠ ③은 둘로 갈린다 — **관측은 같은데 처방이 정반대다** (2026-08-04 athsra)

«심었는데 통과했다» 는 두 가지 다른 원인을 갖는다:

| | 무엇이 틀렸나 | 처방 |
|---|---|---|
| **③-a** | **가드가 무는 것이 옳지 않다** | 가드를 고친다 |
| **③-b** | **결함이 검사 표면 안에 없다** | **가드를 고치면 안 된다** — 대조쌍을 고친다 |

athsra 실측(③-b): E2E 스펙이 단언하는 한국어 문구가 `ko.json` 에 남아 있는지 보는 게이트에
`org_remove` 문구를 심었더니 통과했다. **가드는 정확히 옳게 동작했다** — 몇 분 전에 그 스펙을
`m.org_remove()` 로 바꿔 둬서 그 문구는 **더 이상 리터럴이 아니었다.** 즉 결함을 **검사 대상
밖에** 심은 것이다. 아직 리터럴인 문구로 다시 재니 2건을 잡았다.

**갈라 놓지 않으면 멀쩡한 가드를 넓히다가 오탐을 만든다.** 판별 질문 하나가 가른다:
**"내가 심은 그 결함이, 이 가드가 실제로 들여다보는 대상 안에 있는가."**

##### 그리고 ③은 **새 가드에만 오는 게 아니다** — 매처가 이름보다 좁다 (2026-08-04 atelier)

**가드가 «있다» 는 가드가 «그것을 본다» 가 아니다.** atelier 실측: 「시각 포맷 클래스 가드」가
2026-07-31 부터 **정확히 그 클래스**(런타임 dayPeriod 의존)를 위해 존재했고 *"검사 표면이
비어 있지 않다"* 단언까지 갖추고 있었다. 그런데 매처가 한 글자 좁았다:

```js
/toLocale(?:Time)?String\(/         ← 그때. Date 가 없다
/toLocale(?:Date|Time)?String\(/    ← 고친 것
```

`toLocaleDateString` 에 `hour`/`minute` 를 넣는 건 **정상 동작하는 JS** 다 — 메서드 이름이
아니라 **옵션**이 출력을 정한다. 그래서 결함이 사는 내내 가드는 초록이었다
(측정: 옛 정규식 + 원 사건 → exit 0 · 새 정규식 + 원 사건 → exit 1 · 새 정규식 + 정상 → exit 0).

→ **이미 있는 가드도 원 사건으로 다시 재야 한다.** 「그 클래스를 위해 만들었다」는 기록이
「그 클래스를 덮는다」를 보장하지 않는다. 특히 **매처가 클래스 이름보다 좁은지**를 본다.

###### 그래서 대조쌍의 결함 입력은 **합성하지 않는다** (2026-08-04 atelier, 처방)

위 사례가 ①②③ 어디에도 안 걸린 이유가 처방을 정한다. ①은 통과한다 — **가드의 패턴을 읽고
결함을 만들면 `toLocaleTimeString` 을 만들게 되고 그건 잡힌다.** ②도 통과한다(실제로 심겼다).
③도 아니다 — 가드가 무는 대상은 **옳다.** 좁을 뿐이다.

> **합성 결함을 만들 때 우리는 가드의 패턴을 먼저 읽는다. 그래서 패턴의 좁음이 대조쌍에
> 그대로 복제된다.** 결함이 통과해도 가드는 자기가 틀렸음을 배울 수 없다.

- 결함 입력은 **원 사건 코드를 `git revert`/`git show` 로 되돌려** 쓴다. 원 사건이 있는데
  합성하는 것은 **더 쉬워서**이지 더 정확해서가 아니다.
- 합성이 불가피하면 **가드 소스를 읽지 않은 상태에서** 결함을 쓴다(다른 세션·다른 사람·
  사건 서술만 보고). 패턴을 본 뒤 만든 결함은 그 패턴의 사본이다.
- ⚠ **특히 «범위/대상» 결함은 합성으로 절대 안 잡힌다** (atelier 2026-08-04, 같은 날 세 번째
  실물). 합성자는 게이트의 대상 목록(`SCAN_DIRS`·glob·allowlist)을 **읽고 그 안에 심는다.**
  **원 사건은 대상 밖에 있었기 때문에 사건이 된 것이다.** atelier 실측: TZ 게이트가
  `{pages,components,layouts}` 만 봤고(*"서버는 하이드레이션이 없고 응답은 ISO 로 나가므로
  포맷은 화면 몫"* 이라는 전제), 정작 반례가 서버에 있었다 — `folio-work-projection.ts:29`
  가 카드 **제목**을 완성된 한국어 날짜 문자열로 만들어 응답에 실었다. 범위를 넓히니
  스캔 **93 → 271 파일**. 화면 파일에 합성 결함을 심었으면 **범위 구멍을 영원히 못 본다.**

이 저장소도 같은 밤 같은 것을 밟았다 — `verify:rule-commands` 의 매처가 `bun run` 만 봐서
**`.claude/rules/knowledge.md:11` 이 처방하는 `bunx modfolio-harness-pull` 을 한 번도 본 적이
없었다.** 내 대조쌍은 내가 쓴 정규식을 보고 만들었으므로 영원히 통과했을 것이다.

- ⚠ **그리고 「범위」만이 아니다 — «판정 기준» 결함도 합성으로는 안 잡힌다**
  (atelier 2026-08-05, 위 규칙이 등재된 이유보다 **넓게** 작동한 실증).

  atelier 가 「write-only 표」 게이트를 만들고 음성 대조로 **`anf_leads` 를 읽는 파일 3개를
  지웠는데 게이트가 초록**이었다. 랜딩의 **쓰기**(`api/lead.ts`)가 «소비자» 로 세어졌기
  때문이다. **그런데 결함이 정확히 그 차이였다 — 쓰기는 있었고 읽기가 없었다.**

  > *"내가 «아무 파일이나 하나 지운다» 로 대조를 세웠다면, 참조가 사라지므로 게이트가
  > 물었을 것이고 나는 **teeth 가 있다고 결론지었을 것이다.** 그리고 그 게이트는 실전에서
  > 아무것도 못 잡는다."*

  여기서 대상 집합은 **맞았다**(선언된 표 전체). 틀린 것은 **무엇을 «소비» 로 셀지**였다.
  → **합성 결함은 작성자가 «이미 옳다고 믿는 기준» 에 맞춰 만들어진다.** 그래서 기준
  자체가 틀렸을 때 합성으로는 **원리적으로** 드러나지 않는다. 원 사건은 «대상이 맞는가»
  만이 아니라 **«판정 기준이 맞는가»** 도 시험한다 — 그게 원 사건을 고집하는 두 번째 이유다.

##### 네 번째 형태 — 검사가 **«남의 대상»** 을 봤다 (2026-08-04 athsra)

대상이 비지도(①) 좁지도 가짜(②)도 아니고 **진짜인데 우리 것이 아닌** 경우가 있다.

```
Port 4321 is in use, trying another one...
┃ Local    http://127.0.0.1:4322/            ← 서버는 4322 로 옮겼는데
navigating to "http://127.0.0.1:4321/ai/"    ← 검사는 4321(남의 프로세스)을 봤다
```

하네스가 **요청한 포트를 그대로 믿었다.** 결과는 양방향으로 거짓이다 — 남의 앱이 건강하면
우리 결함이 초록으로 지나가고, 남의 앱이 아프면 우리가 멀쩡한데 빨개진다.
→ 검사 대상의 **실제 주소를 프로세스 출력에서 받아 쓴다.** 요청한 값이 아니라 **배정된 값**이다.
(이 워크스테이션은 형제 세션이 상시 여러 개 도는 곳이라 특히 해당한다.)

⚠ **배포되는 검사기는 이 형태를 «설치되는 순간» 갖는다** (허브 2026-08-04). 위가 포트라면
이건 **디렉터리**다 — 감사 게이트가 루트를 **스크립트 위치 기준**으로 잡고 있었다:

```ts
const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..');
```

허브에서는 맞는다. 그런데 이 파일은 tarball 로 실려 멤버의
`node_modules/@modfolio/harness/scripts/` 로 간다 — 거기서는 **하네스 패키지 자신을 감사**하고,
멤버는 자기 트리가 검사된 줄 안다. 배선 직전에 잡았고, 안 잡았으면 **30 repo 에 거짓 초록을
배포**할 뻔했다. → 배포되는 검사기의 대상은 **호출한 프로젝트(cwd)** 에서 찾고, 그 결과가
`node_modules` 안이면 **판정 불능(2)** 으로 흘린다. 그리고 대조쌍은 **멤버 레이아웃을
저장소 밖에 재현해서** 돌린다 — 허브에서만 돌리면 이 결함은 원리적으로 안 보인다.

### 게이트가 **선언한 전제**가 충족되는지는 결과로 검증되지 않는다 (2026-08-04 athsra)

게이트가 자기 유효성의 조건을 **코드로 선언**하고 주석에 **왜 필요한지까지** 적어 두는데,
그 선언이 **실제로는 안 걸리는** 경우가 있다. 대상도 맞고 검사도 돌고 결과도 진짜다 —
**그 결과가 유효하려면 필요했던 조건이 없었다.**

athsra 실측: axe 스펙이 `test.use({ reducedMotion: 'reduce' })` 로 세워져 있었고 이유도
정확했다(*"진입 애니메이션이 `opacity:0` 으로 시작하면 axe 가 «숨은 요소» 로 건너뛰어
위반이 조용히 누락된다"*). 페이지 안에서 재니:

```
matchMedia('(prefers-reduced-motion: reduce)').matches       → false
document.getAnimations().filter(a => a.playState==='running') → 6
```

| 어떻게 세우나 (Playwright 1.62.1 chromium) | 결과 |
|---|---|
| 프로젝트 `use.reducedMotion` · 최상위 `use` · `test.use` | **false** |
| `page.emulateMedia({ reducedMotion })` · `browser.newContext({ … })` | **true** |
| **`use: { contextOptions: { reducedMotion: 'reduce' } }`** (중첩) | **true** ★ |

⚠ **같은 `use` 블록의 `viewport`·`colorScheme` 은 정상 적용된다** — «`use` 가 통째로
무시된다» 가 아니라 **이 옵션만** 안 간다.

### ★ 근본 원인 — **문서화된 형태가 조용히 버려진다** (athsra 2026-08-04, 저장소 밖 최소 재현)

평평한(문서화된) `reducedMotion` 은 컨텍스트에 도달하지 않고, **중첩된 `contextOptions`**
는 도달한다. 같은 `use()` 호출 안에서 갈린다:

```ts
base.use({ colorScheme: 'dark', reducedMotion: 'reduce' });
// → prefers-color-scheme: dark    = true    ← 배선은 살아 있다
// → prefers-reduced-motion:reduce = false   ← 이것만 안 간다
```

**«설정 실수» 가 아니라는 증거가 이 한 줄이다** — 같은 객체의 다른 키가 가는데 이 키만
안 간다. athsra 는 파생 픽스처를 먼저 의심했다가 base test 에서도 false 인 것을 보고
빈 프로젝트에 최소 재현을 세워 갈랐다(`@playwright/test` 1.62.1 · chromium · WSL2).

→ **처방**: `use: { contextOptions: { reducedMotion: 'reduce' } }` 또는
`page.emulateMedia(...)`. 그리고 **어느 쪽을 쓰든 전제 자체를 단언한다** — 이 버그가
고쳐지면 평평한 형태가 다시 동작할 텐데, 그때 단언이 없으면 **바뀐 줄도 모른다.**

**★ 왜 오래 사나 — 침묵이 두 원인을 구분해 주지 않는다.** 같은 파일의 `bypassCSP` 는 안
걸리면 **axe 주입이 막혀 테스트가 죽는다** — 결과가 원인을 증명한다. `reducedMotion` 은
반대다: 걸려도 «위반 0», 안 걸려도 «위반 0»(애니메이션 중 요소를 건너뛰어서). **두 0 이
구분되지 않는다.**

→ **전제는 그 자체를 단언한다.** 페이지 안에서 `matchMedia(...).matches` 를 읽거나 실행
중 애니메이션 수를 세는 식으로, **결과가 아니라 조건을 잰다.** 그리고 원인을 못 찾았으면
**실행으로 세우고**(`page.emulateMedia`) 선언은 지우지 말고 문서로 남긴다 — 다음 사람이
«없네» 하고 다시 넣다가 같은 것을 밟지 않게.

### 게이트를 검증할 때는 **실행 형태**까지 재현하라 (2026-07-29 modfolio-infra 제안, 채택)

대조쌍을 세워도 **손으로 돌린 형태**와 **CI 가 실제로 쓰는 형태**가 다르면 그 검증은 다른
프로그램을 검증한 것이다. 실측(infra): `gates:handoff` 첫 실행이 `restore` 만 exit 1 인데
**로그는 세 검사 전부 ✓** 였다. `cat file | pg_restore --list` 에서 pg_restore 가 TOC 만 읽고
먼저 끝나면 `cat` 이 EPIPE 로 죽고 그 예외가 종료코드를 오염시킨다 — **통과한 뒤 실패를
보고**하는 구조다. 그리고 이 결함은 **터미널에서 직접 돌리면 안 보인다**(stdout 이 터미널이라
EPIPE 가 안 난다). 리다이렉트·파이프 환경에서만 드러난다.

> **거짓 음성은 노이즈보다 나쁘다 — 게이트를 무시하도록 사람을 훈련시키기 때문이다.**

→ 새 게이트는 **CI/무인 실행과 같은 형태**로 한 번 돌린다: 출력 리다이렉트(`>> log 2>&1`) ·
파이프 · 비대화형 셸 · cron/헤드리스. 형태를 바꾸면 결과가 바뀌는 검사가 실재한다.

#### ⚠ 그리고 거짓 빨강의 대가는 «무시하도록 훈련» 만이 아니다 — **수습 행위가 진짜 결함을 만든다** (허브 2026-08-05 실측, 자기보고)

위 문장은 거짓 음성의 비용을 **주의력**으로 봤다. 이번에 그보다 큰 비용이 나왔다:
**오판정을 «수습» 하는 행위가 멀쩡하던 것을 실제로 깨뜨렸다.**

경위 — 게시 후 레지스트리 실물을 확인하는 검증을 새로 넣었다(옳은 방향이다). 그런데
**즉시 1회만** 읽게 만들었다. canonical(Forgejo)은 게시 직후 packument 에 버전을 바로
싣지 않는다 — 실측 **약 80초**(20초 간격 5회째에 등재+무결). 그래서:

```
[4/5] npm publish → canonical      npm exit 0     ← 실제로는 성공
      failed: exit 0 인데 3.53.0 이 없습니다      ← 검증이 80초를 안 기다렸다
  ↓ 「게시가 실패했구나」
수동으로 npm pack + publish 재시도  → 409 Conflict
  ↓
결과: 그 버전의 **타르볼이 기록된 sha512 와 불일치**
      packument 에는 있고 `latest` 인데 `bun add` → Integrity check failed
      (3.51.0 · 3.52.0 은 일치 — 그 한 버전만)
```

**그 릴리즈는 검증이 없었으면 정상이었다.** 검증이 만든 오판정이 재게시를 부르고,
재게시가 아티팩트를 깨뜨렸다. 그리고 `latest` 가 그리로 옮겨가 있었으므로 **fleet 전체의
`bun add` 가 깨질 수 있는 상태**였다(즉시 이전 버전으로 되돌려 막았다).

→ **처방 셋**:
1. **전파 지연과 «없음» 은 다른 사실이다.** 원격 상태를 게시·배포 **직후** 확인하는 검사는
   **창을 준다**(폴링). 이건 「판정 불능을 통과로 환원하지 않는다」의 시간 축이다.
2. **같은 세션에서 배운 것을 옆 파일에 적용한다.** 이 저장소는 **몇 시간 전에**
   `published-canary` 에서 정확히 같은 전파 지연을 만나 창을 줘서 고쳤다. 그 교훈을
   **새로 쓰는 검증에 옮기지 않았다.** 교훈은 파일마다 다시 배우지 않는다.
3. **빨간불에 대한 «수습» 도 되돌릴 수 없는 행위면 게이트를 통과해야 한다.** 게시된
   아티팩트에 다시 쓰는 것은 비가역이다 — 재게시 전에 *"정말 실패했는가"* 를
   **독립 경로로** 확인한다(여기서는 packument 를 몇 분 뒤에 다시 읽는 것이면 충분했다).

> 이 저장소가 오래 다듬어 온 축은 «초록을 의심하라» 였다. 이번 건은 그 대칭이 **더 비싸게**
> 나타난 경우다 — **빨강을 의심하지 않아서 멀쩡한 것을 고치다가 깨뜨렸다.**

##### 그리고 그 사고의 **하류 원인** — 「거부했다」를 「아무 일도 없었다」로 읽었다 (2026-08-05, 소스 확인)

재게시가 왜 아티팩트를 손상시켰는지 소스에서 갈렸다(`modfolio-infra registry/proxy`):

```
publish.ts:192  await putTarball(...)     ← ① 타르볼을 무조건 덮어쓴다
publish.ts:205  return mergeVersion(...)  ← ② 여기서 no-clobber 409
```

**no-clobber 는 정상 동작했다.** 409 도 정확히 나왔다. 문제는 그 거부가 **①을 되돌리지
못한다**는 것이다 — packument 는 최초 게시본의 integrity 를 유지하고 R2 는 재게시본의
바이트를 갖는다. 소비자에게는 `IntegrityCheckFailed` 로 나타난다.

전제는 주석에 **적혀 있었다**: *"tarball 은 멱등 overwrite 안전"*. 그 문장은
**«같은 버전이면 같은 바이트»** 를 가정하는데 **`npm pack` 은 바이트 재현이 안 된다**
(gzip mtime/OS 비트). 가정이 깨지는 순간 «멱등» 이 «파괴적» 이 된다.

⚠ **그리고 그것을 지킨다는 테스트가 있었다**:
`test/stage3.test.ts` — *"같은 버전 재publish → 409 no-clobber(**immutable**)"*.
**409 가 나오는지만 단언한다.** 그 시점에 타르볼이 이미 바뀌었는지는 안 본다.
「세 번째 부류」(검사를 만족시키는 가장 싼 방법)의 변종인데, 여기서는 **이름이 보장하는
성질(immutable)과 단언하는 것(상태 코드)이 다르다.**

→ **거부·롤백·취소를 단언하는 테스트는 «거부됐는가» 가 아니라 «아무것도 안 바뀌었는가» 를
단언한다.** 그리고 그 음성 대조는 **바이트가 다른 두 입력**이어야 한다 — 같은 바이트로
재시도하면 결함이 있는 코드도 통과한다(그게 이 결함이 오래 숨어 있던 이유다).

##### ⚠ 그리고 **«반증» 도 실물로 해야 한다 — 추론으로 한 안심이 더 나빴다** (허브 2026-08-05, 형제가 반증)

같은 사고의 후속. 손상본을 fleet 에 방송할지 정하려고 「경보 전 반증 1스텝」을 밟았고,
**«노출 0» 이라고 저널에 적었다.** 근거 셋: `dist-tags.latest` 를 되돌렸다 ·
`origin/main` 의 tip 이 그 버전이었던 적 없다 · 창이 15분이고 실패가 시끄럽다.

**30분 뒤 형제가 실제로 막혔다.** 빼먹은 사실 하나:

> **`^3.x` 는 `dist-tags` 를 보지 않는다 — 최고값을 고른다.**
> `latest` 를 되돌려도 범위를 가진 멤버는 여전히 손상본을 해석한다.

그리고 **내 카나리아가 통과한 이유가 정확히 그것**이다 — 카나리아는 범위 없이
`bun add` 하므로 `latest` 를 따라간다. **멤버는 범위를 갖고 있다.** 즉 내 검사는
«소비자가 받는 것» 을 잰다고 선언했지만 **소비자와 다른 방식으로** 물었다.

→ **처방**: 「경보 전 반증」의 반증도 **실물 측정**이어야 한다. 세 값을 재고 네 번째를
**유도**하면 그건 반증이 아니라 추론이다. 여기서 실물은 «멤버 `package.json` 의 범위로
콜드 설치» 한 줄이었다(실측 후 3.53.1 로 해소 확인: `^3.50.0` → 3.53.1 설치 성공).

⚠ **경보를 안 보낸 것보다 «괜찮다» 를 문서에 적은 것이 나빴다.** 침묵은 나중에 고쳐지지만
**기록된 틀린 안심은 다음 사람의 조사를 멈춘다**(「한계 기록이 다음 진짜 0건도 설명해
버린다」와 같은 뿌리).

#### **형태**만이 아니라 **환경 상수**도 갈린다 — `TZ` (2026-08-04 atelier, fleet 실측)

가장 조용한 축은 로케일·시간대다. `toLocaleDateString("ko-KR")` 에 `timeZone` 을 안 주면
**호스트 TZ** 를 쓴다:

```
같은 순간, timeZone 없이
  로컬 개발(KST) → 2026년 10월 15일 00:00
  Cloudflare Workers (UTC) → 2026년 10월 14일 15:00   ← 하루가 밀린다
```

24시간 중 **9시간**이 어긋나고, **개발 박스가 `Asia/Seoul` 이라 로컬에서는 영원히 안 보인다.**
게이트·e2e 의 초록이 «없다» 가 아니라 **«안 봤다»** 인 전형이다. 그리고 SSR·메일·PDF·CSV 처럼
**서버 전용 경로는 하이드레이션 경고조차 안 난다** — 조용히 틀린 값이 고객에게 나간다
(atelier 실측: pay 의 구독 갱신 안내 메일, 영수증 CSV).

허브 fleet 실측 2026-08-04(읽기 전용 정적 스캔, `toLocale(Date|Time)String(` 만):

```
modfolio-connect 22 · gistcore 18 · naviaca 19 · modfolio-press 18 · dle-desk 15
atelier 14 · pdgd 7 · modfolio-pay 5 · modfolio-admin 5 · umbracast 5 …
```

⚠ **첫 스캔은 틀렸다.** `toLocale(Date|Time)?String\(` 로 재서 **숫자 천단위
`toLocaleString()` 까지 세었고**, 그 결과 이 저장소가 «17건» 으로 나왔다. 실제로는 **0건**
(전부 토큰 수 포맷). 위 「매처가 이름보다 좁다」의 **거울상** — 매처가 이름보다 **넓으면**
남의 결함을 우리 것으로 만든다. 옵션 구조로 걸러야 오탐이 0이 된다.

→ 재현 환경은 **형태 + 환경 상수**다: `TZ` · `LANG`/`LC_ALL` · `Intl` 데이터 유무.
프로덕션이 UTC 면 게이트도 `TZ=UTC` 로 한 번 돌린다.

##### ⚠ «서버냐 브라우저냐» 를 가르려 하지 마라 — **갈라도 답이 같다** (atelier 2026-08-04)

이 축의 게이트를 만들면 곧바로 *"이 코드가 서버에서 도는가 브라우저에서 도는가"* 를 묻게
된다. 정적 스캔으로는 (`+server.ts` 같은 파일명 규약을 빼면) **원리적으로 알 수 없다.**
그런데 **알 필요가 없다** — 세 경우 전부 틀리기 때문이다:

| 실행 위치 | 시간대를 런타임에 맡기면 |
|---|---|
| 서버 (Workers = UTC) | 값이 **그냥 틀리다**. 경고 없음 |
| 브라우저 (KST) | 값은 맞지만 SSR 과 갈려 **하이드레이션 불일치** |
| 빌드 시 (Astro prerender 등) | **빌드 머신 TZ 가 굳는다** |

→ 규칙은 「어디서 도는지 알아내라」가 아니라 **«런타임이 시간대를 고르게 두지 않는다»** 다.
게이트 범위에서 서버 코드를 면제하지 않는다 — atelier 가 정확히 그 면제를 했다가
자기 반례를 놓쳤다(위 §범위 결함).

**보는 사람의 시간대가 정말 필요한 앱**(글로벌 SaaS 활동 로그 등)도 규칙은 같다:
시간대 선택이 **명시적**이어야 한다 — `timeZone: <고정값>` 이거나 「viewer-local 이다」라고
이름 붙은 헬퍼이거나. **암묵은 없다.** 암묵이 곧 «런타임이 골랐다» 이고, 그건 서버·브라우저·
빌드에서 각각 다른 답을 낸다. atelier 는 한 발 더 갔다 — 시간대를 **함수 인자에서 없앴다**
(`STUDIO_TIME_ZONE` 상수). *"빠뜨릴 수 있는 것을 인자로 두지 않는다."*

###### ⚠ 다만 **값을 일괄로 강제하면 절반이 망가진다** (pay 2026-08-04, atelier 와 갈린 판단)

pay 실측: 원시 호출 6건은 표면이었고 뿌리는 중앙 포매터였다(`formatDate` **호출부 95건 /
38파일**, 다섯 포매터 전부 `timeZone` 없음). 그런데 **KST 일괄 치환은 틀렸다:**

| 무엇 | 정답 | 왜 |
|---|---|---|
| 결제일·갱신일·영수증 | **KST** | 서울의 10/15 00:30 은 그 사람 달력으로 15일 |
| **청구 기간 라벨** | **UTC** | `getBillingPeriodStart` 가 만든 **UTC 월 경계**에서 나온 값. KST 로 표시하면 **8월 청구서가 «7월 31일부터» 로 보인다** |

두 번째 칸은 **이미 맞게 돼 있었다** — 강제했으면 고치는 게 아니라 깨뜨리는 것이었다.

→ **규칙은 두 층이다**: ① 런타임이 고르게 두지 않는다(예외 없음) ② **어떤 값이 맞는지는
그 데이터의 의미가 정한다** — 게이트는 **명시를 강제하고 값은 강제하지 않는다.**
pay 의 문구: *"UTC 가 맞는 값이면 `timeZone: \"UTC\"` 를 적는다 — **적는 것 자체가 요건이다.**"*
앱 성격상 하나가 정답인 곳(atelier = 학원 KST)은 상수로 못 박아도 되지만, **데이터가 UTC
경계로 만들어졌으면 그 표시는 UTC 가 맞다.**

⚠ **그러므로 「원시 호출 0개 · 예외 목록 없음」은 일반 규칙이 아니라 특수화다** (atelier
자기 정정 2026-08-04, pay 가 반증한 뒤). *"정답이 하나뿐인 도메인에서 «명시» 를 «한 곳에
모음» 으로 강화한 것"* 이고, **그 조건이 없는 앱이 복사하면 값을 틀리게 강제한다.**
일반형은 어디까지나 **«명시를 강제하라»** 다.

⚠ 프로브 함정(pay): `timeZone` 유무를 **여는 줄만 보면** 옵션 객체가 여러 줄일 때
**있어도 «없다» 로 읽는다.** 괄호 균형으로 호출 전체를 잘라 판정하고, **음성 대조에 그
여러 줄 형태를 일부러 심어** 확인한다.

#### **관찰 창이 사건보다 먼저 닫히면** 침묵이 «결함 없음» 이 된다 (2026-08-04 atelier)

수집기를 붙였는데 초록인 것은, 수집기가 **언제까지 보고 있었는지**를 확인하기 전까지
결과가 아니다. atelier 실측: a11y 스펙 50여 라우트에 `pageerror`/`console` 수집기가
**하나도** 없어서 배선했더니 첫 실행에 3건이 나왔다(그중 하나는 **핸들러가 없어 404 인 GET**
을 호출부가 `catch {}` 로 삼키고 화면은 «데이터 없음» 으로 렌더 — 기능이 통째로 죽은 채
게이트는 초록이었다).

⚠ **그런데 처음 배선했을 때도 초록이었고 그게 틀린 초록이었다.** 일부러 심은
`console.error` 가 안 잡혔다 — `waitUntil: "domcontentloaded"` 가 **하이드레이션 전에**
반환한다. 음성 대조가 아니었으면 「런타임 결함 게이트 신설 완료」로 끝났을 자리다.

→ 새 수집기·리스너·구독은 **관찰 창이 사건을 포함하는지**를 먼저 단언한다
(`waitUntil` · 타임아웃 · 언마운트 · 프로세스 종료 시점). 이건 위 「예산 순서」(중첩된
타임아웃은 안쪽이 더 짧아야)의 **관찰 쪽 얼굴**이다.

##### 부분 이관은 «고쳤다» 가 아니라 «고친 자리만 고쳤다» 다

atelier 가 같은 노트에서 세운 규율이고 실측이 뒷받침한다 — 2026-07-31 에 util 을 만들고도
**19곳 중 11곳이 미이관**이었다. 그래서 «`timeZone` 만 있으면 통과» 로 게이트를 만들지
않았다: 통과 형태가 둘로 갈리면(util 을 쓰는 코드 / 손으로 적은 코드) **후자가 다음 사람이
빠뜨리는 자리**가 된다. 원시 호출을 **0개**로 만들고 예외 목록을 두지 않는다 — 필요한
형태는 util 에 함수로 추가한다.

> **이관을 끝내는 것은 게이트이지 의지가 아니다.**

허브 실측이 이걸 그대로 보여준다: gistcore 에는 `lib/util/kst.ts` 가 *"the single source of
truth for the JS-side"* 라고 선언하며 **이미 있는데**, import 하는 프로덕션 파일은 **2개**
(`server/queries/analytics.ts` · `server/usage.ts`)이고 화면 쪽 18곳은 원시 호출이다.
**해결책을 만든 것과 이관이 끝난 것은 다른 사건이다.**

### 판정 순서를 뒤집는 지름길이 문서와 반대로 적혀 있을 수 있다 (2026-07-30 허브 실측)

허브의 `feedback-direction.ts` 는 문서에 *"content beats filename, because the filename is
exactly what went wrong"* 이라고 적어 두고, 정작 코드는 **파일명 검사를 본문 검사보다 먼저**
돌리고 있었다. 그래서 `ecosystem-opinion-20260729-six-**findings**-verification.md`(첫 줄
`# ecosystem → modfolio-connect`, 명백한 허브 회신)가 슬러그 안의 단어 하나로 **member 인바운드**가
됐다. 결과는 **영구히 멈추지 않는 카운터**다 — 허브가 자기 회신에 답할 수는 없으므로 그 1건은
영원히 "미회신" 으로 남는다. 미회신 수는 **무슨 일이 존재하는지를 정하는 숫자**라, 이건 작은
오차가 아니다.

→ 규칙: **주석이 선언한 우선순위를 코드가 지키는지 테스트로 잠근다.** 그리고 카운터가 **줄지
않는 항목**이 있으면 대상이 아니라 분류기를 먼저 의심한다.

### 라이브러리의 "판정 불능" 이 **참**으로 새어 들어올 수 있다 (2026-07-30 허브 실측)

```ts
Bun.semver.satisfies('7.0.2', 'not-a-range')  // → true
```

해독 불가한 range 가 조용히 "만족함" 이 된다 — 판정 불능이 통과로 환원되는 형태인데, 이번엔
**우리 코드가 아니라 라이브러리 안**에 있다. 그래서 우리 쪽 대조쌍만으로는 안 잡힌다.

→ 규칙: 외부 판정 함수에 **신뢰할 수 없는 입력**(레지스트리 문자열·사용자 값·원격 응답)을
그대로 넘기기 전에, **해독 불가를 별도 상태로 먼저 분류**한다. 그리고 그 라이브러리가 쓰레기
입력에 무엇을 돌려주는지 **테스트에 한 줄로 박아 둔다** — 동작이 바뀌면 그 줄이 알려준다.

### 게이트가 프로덕션 러너와 **다르게 파싱하면** 아무도 적용하지 않는 스키마를 축복한다 (2026-07-31 modfolio-connect)

infra 의 "실행 형태까지 재현하라" 와 같은 축인데, 이쪽은 **파서**가 갈린다.

실측: 마이그레이션 러너가 `sql.split(";")` 로 문장을 자르는데, 그건 **`--` 주석 안의 `;`**
에서도 자른다. 게다가 문장별 실패를 abort 대신 `errors[]` 로 삼켜서, 결과는 **절반만 적용된
마이그레이션이 "경고 목록"으로 보고되는** 상태다. 그런데 검증 게이트가 그와 **다른 파서**로
같은 파일을 읽으면, 게이트는 *프로덕션이 실제로 적용할 스키마가 아닌 것*을 축복한다.

→ 규칙: 스키마·설정·마이그레이션을 검증하는 게이트는 **프로덕션 러너와 같은 파서**를 쓰거나,
같은 입력에 대해 두 파서의 산출이 동일한지를 테스트로 잠근다. 그리고 **문장별 실패를 모아
경고로 내리는 러너는 부분 적용을 성공으로 보고한다** — 그 자체가 결함이다.

### "서명이 맞다" 는 "우리가 발급했다" 이지 "**이 용도로** 발급했다" 가 아니다 (2026-07-31 modfolio-connect)

같은 키로 서명한 토큰이 두 종류 이상이면 **서명 검증만으로는 구분이 불가능**하다. Connect 는
ID-JAG 표명과 일반 액세스 토큰을 같은 키로 서명하는데, `typ` 를 안 봤다면 **그 서버가 발급한
아무 액세스 토큰이나** jwt-bearer 로 제시해 다른 앱용 토큰으로 교환할 수 있었다.

→ 규칙: 토큰을 받아들이는 쪽은 서명·`iss`·`aud` 에 더해 **`typ`(또는 등가의 용도 클레임)**
을 검사한다. 자체 AS 를 돌리는 repo 에 특히 해당한다. 이건 암호학 문제가 아니라 **범위**
문제다 — 같은 열쇠가 여는 문이 둘인데 문패를 안 본 것이다.

---

# 2026-08-24 — 하룻밤에 만든 게이트 넷이 **자기 자신에게 여덟 번** 걸렸다

CLAUDE.md 의 `quality:all` 행이 가리키는 서사가 여기 있다. 공통 형태 하나:
**「언급」을 「행위」로 세는 것**, 그리고 **신호가 좁아서 결론이 뒤집히는 것**.

## `ai:census` — 네 번

런타임 LLM 호출 인구조사. *「AI 가 코드를 쓴다」와 「결과물이 AI 를 요구한다」는 다르다* 를
검사 가능하게 만든 게이트다. 그런데 만드는 동안 네 번 자기에게 걸렸다.

**① 정규식 리터럴을 호출로 셌다.** `CALL_PATTERNS` 배열의 `/\bfrom\s+['"]openai['"]/` 가
자기 패턴에 매치했다. → 면제 목록이 아니라 **구조**로 고쳤다(정규식 리터럴 중화).
이름을 예외에 적으면 다음에 탐지기를 만드는 사람이 같은 자리에서 다시 빠진다.

**② 블록 주석 제거가 줄 수를 줄여 행 인덱스가 전부 어긋났다.** 실측 **309행 → 200행**.
결과가 셋이었다: 매처가 엉뚱한 줄을 검사했고, 리포트 줄번호가 전부 거짓이었고,
**선언 탐색창이 다른 데를 봤다.**

> 세 번째가 가장 나쁘다 — 게이트가 **정직하게 선언한 사람을 벌한다.**
> *"규약 준수를 벌하는 게이트는 채택을 직접 막는다."*

→ 규칙: **줄 단위로 보는 모든 스캐너는 주석을 개행만 남기고 지운다.**

**③ `why:` 설명 문자열을 import 로 셌다.** `why: "import from 'openai'"` 가 매치했다.
「언급」과 「모듈 해석」을 가르는 것이 그 게이트의 목적인데 초판은 **언급을 세고 있었다.**
→ import 는 **실제 import/require 문**일 때만.

**④ ★ 거짓 초록을 배포했다 — 38% 미탐.** 「허브 미선언 0」으로 내보냈는데 그때도 허브는
`api.moonshot.ai`(Kimi)와 ollama 를 부르고 있었다. *"알려진 양성 1건을 잡는지 먼저 통과시켜라"*
를 **안 하고 배포했다.**

> **적어 둔 한계와 있는 줄 몰랐던 구멍은 다른 것이다.** 전자는 고지이고 후자는 결함이다.

세 번 확장해서 40 → **65 지점**(8 → 13 repo)이 됐다:
- 벤더 호스트 → **와이어 프로토콜 경로**. 호스트 목록은 끝없이 새고 **프로토콜은 유한하다**
- `fetch` 문법 → **엔드포인트 URL 자체**. `judge.ts` 는 fetch 가 아니라 **curl argv 배열**이었다 —
  「호출 문법」으로 잡는 한 **서브프로세스는 원리적으로 안 보인다**
- 분류에 **`instrument`** 추가. 허브 5건 중 **넷이 제품 기능이 아니라 계측**이었다(벤치·프로브·A/B).
  억지로 `language` 로 적었으면 **분류가 거짓이 되고** 나중의 부품화 판단을 오염시킨다

## `verify:bunx-leak` — 증상에 이름이 잘못 붙어 3개월을 살았다

`quality:all` 이 두 단계에서 연속으로 죽었고 **둘 다 같은 뿌리**였다:

```
typecheck:loom   InstallFailed @typescript/typescript-win32-x64 · EPERM NtSetInformationFile
test:dashboard   EISDIR: watch '//wsl.localhost/Ubuntu/home/mod/code/…/dist/client'
                 at C:\Users\…\Temp\bunx-…-wrangler@latest\…
```

```
which -a bunx →  /mnt/c/Users/…/AppData/Roaming/npm/bunx   ← Windows npm shim
which -a bun  →  /usr/local/bin/bun                        ← WSL 네이티브
```

**`bun` 은 WSL 인데 `bunx` 만 PATH 에 없어 Windows 로 떨어진다.**

⚠ CLAUDE.md 는 이 현상을 이미 적어 뒀다 — *"bunx 는 로컬 bin 을 건너뛰고 404 를 낸다 …
404 나면 재시도"*. **재시도는 처방이 아니다.** bun 의 버그가 아니라 **PATH 해석**이기 때문이다.

> **증상에 이름을 붙이면 원인 조사가 멈춘다.** 그 이름이 3개월을 살았다.

게이트는 **구조 축만 red**(로컬에 있는 것을 bunx 로 부름), 환경 축은 advisory —
머신마다 다르므로 red 로 만들면 정상 머신에서 **거짓 빨강**이 된다.

**⑤ 그 게이트의 대조쌍이 가드 자신의 구멍을 잡았다.** 초판이 `exec()` 라 **줄당 첫 매치만**
봐서 `bunx a && bunx b` 의 둘째를 놓쳤다 — **결함을 심었는데 exit 0** 이 나왔다.
그 0 을 「가드가 옳다」로 읽었으면 구멍이 그대로 살았다.

## `verify:user-facing` — 가정을 재는 게이트가 자기 매처에 걸렸다

속도 doctrine **전체**(커밋 게이트 제거·PR 폐기·훅 2개)가 *"무사용자 1인 pre-production"*
한 가정 위에 있다. 실측하니 **트리거는 있고 계측기가 없었다** — `ecosystem.json` 사용자 필드
**0개** · `harness-lock.json` 보유 **0/33** · `liveHosts` **전부 빈 배열**(한 번도 안 채워졌다).

**⑥ 첫 실행 오탐이 census 의 가치를 동시에 보여 줬다.** census 가 **내 손 스윕이 놓친 repo**
(gistcore, 공개 도메인 보유)를 찾았다 — 유한한 방향에서 일한 것은 옳았다. **그런데 찾은 것이
가입 라우트가 아니었다**: `routes/api/classes/join` = **수업 참여**.

실측으로 갈랐다 — `join` 을 빼면 전 fleet **0건**, `join` 이 잡은 것은 그 오탐 **하나뿐**.
「빼면 검출력 0」과 「두면 오탐 1」 사이를 **원칙**으로 갈랐다:

> **사용자 가입은 인증 경계 근처에 살고, 리소스 참여는 리소스 아래 산다.**

검토가 들었던 반증 둘도 재 보니 약했다 — `posthog-js` 는 **키 미설정 → no-op**(아무도 추적
안 됨), `@athsra/cli` 월 955 는 중앙값 13/일·0인 날 2·244 스파이크 1회 = **미러/CI 모양**이고
무엇보다 **npm 다운로드는 사람과 봇을 원리적으로 못 가른다** → 「0명」이 아니라 **판정 불능**.

**⚠ 이 파일을 쓰다 `pre-payment-guard` 에 걸렸고 — 가드를 안 건드렸다.** 결제 탐지 패턴의
승인 경로 **문자열**을 가드가 호출로 읽었다(오탐). **돈 가드에서 과탐은 안전한 방향**이고
작성자 편의로 좁히는 것은 정확히 잘못된 거래다. 패턴을 조각에서 조립했다 —
**가드는 그대로, 내 쪽이 비킨다.**

## `verify:cf-scope` — 세 번 재고서야 맞았고, 두 번의 오답은 위험했다

```
① CLOUDFLARE_API_TOKEN 을 코드에서 참조하는 repo   →  9   → 「26곳이 안 쓰면서 들고 있다」
② package.json 의 wrangler 스크립트까지            → 26   → 「5곳」
③ wrangler.jsonc **존재**까지                      → 32   → **「0곳」**
```

③이 맞다 — **CF Workers Builds 로 배포하는 repo 는 로컬에서 wrangler 를 부르지 않는다.**

> **신호가 좁으면 결론이 뒤집힌다.** 그리고 이 경우 그 결론으로 토큰을 회수하면 **배포가 죽는다.**

폭의 갭은 **0** 이었다. 진짜 문제는 **깊이**다 — 메가토큰은 **353 권한·만료 없음**이고
`wrangler.jsonc` 바인딩에서 도출한 실제 필요는 repo 당 **1~8개**(전체 권한 종류 **9**).

**⑦ 권한 이름을 지어내지 않았다.** CF 카탈로그를 실측(`GET /user/tokens/permission_groups`,
**395개**)해서 매핑했다. 그래서 `Cloudflare Pages` 도 `Workers Builds` 도 **없다** —
카탈로그에 그 이름이 없기 때문이다(Builds 는 `Workers CI`).
**추측한 이름으로 만든 토큰은 조용히 부족해진다.**

⚠ 그 실측이 메모리도 정정했다 — 저장된 사실이 카탈로그를 **366** 이라고 적고 있었는데
실제는 **395** 였다. *"관측 시점에 얼어붙는 표는 반드시 늙는다."*

## ⑧ 그리고 `context:budget` 이 나를 막았다

위 넷의 서사를 CLAUDE.md 에 쌓았더니 **40,602 / 40,000 bytes** 로 상한을 넘겼다.
`instruction-drift` 가 막으려는 **정확히 그 행동**을 내가 하고 있었다.

게이트 메시지가 처방까지 줬다 — *"새 교훈은 append 가 아니라 셋 중 하나로 간다:
① 게이트 ② Muse ③ 증례 파일"*. 그래서 이 문서가 있다. CLAUDE.md 에는 포인터만 남겼다
(40,602 → **37,744**).

> **상한을 올려서 통과시키는 것은 처방이 아니다. 「예산이 늘어난 기록」은 게이트가 아니다.**

---

## 이 여덟 건의 공통 규칙

1. **통과만 보면 여섯 개가 다 살아남았다.** 잡은 것은 전부 **대조쌍·변이 주입·기준선 대조**다.
2. **오탐을 고칠 때는 검출력 보존을 같은 테스트에 넣는다.** 「사라졌다」만 보면 그 수정이
   랭킹까지 지웠는지 안 보인다 — 실제로 한 번 지웠다(동의어 무리 과확장 → 「실험」 1위 강등).
3. **신호가 좁으면 결론이 뒤집힌다.** 26 → 5 → 0. 세 번째가 맞았다.
4. **적어 둔 한계 ≠ 있는 줄 몰랐던 구멍.** 전자는 고지이고 후자는 결함이다.
5. **안전 가드가 오탐을 내면 가드가 아니라 내 쪽을 고친다** — 적어도 돈·파괴 축에서는.

---

## 2026-08-24 — 밤샘 세션의 게이트 셋 (CLAUDE.md 에서 무손실 이관)

> `context:budget` 이 CLAUDE.md **40,489/40,000** 에서 막았다. **한도를 올리지 않고** 서사를 여기로 옮긴다 —
> `instruction-drift.md` 의 라우터 그대로(검사 가능→게이트 · 판단 휴리스틱→Muse · 실측 서사→증례).
> **한 글자도 버리지 않았다.** 같은 일이 이 세션에서 두 번째다 — 서사는 CLAUDE.md 에 쌓이려는 성질이 있다.

### `verify:retired-approach` · `verify:supply-chain` · `verify:debrief-sink`

⓪ `verify:retired-approach` — **폐기된 접근이 «진행 중» 으로 살아 있는가.** README 는 자기 독자를 *"Audience: **AI Agents**"* 라고 적어 놓고 로드맵에 `| GitHub Actions 템플릿 | 🔲 대기 |` 를 **두 달간** 두고 있었다 — 배포 표준은 2026-06-30 에 CF Workers Builds 로 확정됐고 **GHA 배포는 금지**다. dotenvx 는 `🔲 진행 중` 으로 **4개월**(실제로는 athsra v3 로 대체). ⚠ **아무 게이트도 이 표면을 안 봤다** — 타입·린트·테스트는 코드를 지키지만 *사람과 에이전트가 읽고 그대로 구현하는 산문*은 아무도 안 본다. ⚠ 규칙을 **세 번** 고쳤고 매번 **원 사건**이 고쳐 줬다: ① 줄 단위 면제 → 「Doppler 폐기」가 같은 줄 dotenvx 를 삼킴 ② 최근접 용어 귀속 → **정정된 행 2개를 물었다**(과탐이 규약 준수를 벌한다) ③ **상태를 «상태가 적히는 자리»에서 읽는다**(표=마지막 칸 · 산문=문장). 합성 결함이면 셋 다 안 나왔다. 2026-08-24 +3: ⓪ `verify:supply-chain` — `@modfolio/*` 소비의 **검증 가능한 축**. ⚠ 이 게이트의 요점은 초록이 아니라 **못 보는 축을 매번 같이 찍는다**는 것이다: `pkg.modfolio.io`(Forgejo)는 packument 에 **서명·provenance 를 주지 않는다** — npmjs zod 를 알려진 양성으로 둔 동일 probe 대조(그쪽은 `dist.signatures`+`attestations`, 우리는 `integrity/shasum/tarball` 뿐) + 1차 출처(Forgejo npm 문서에 provenance/SLSA/Sigstore 언급 **0**). **부채가 아니라 구조다** — 워크스테이션 게시라 npmjs 로 가도 의미 있는 provenance 를 못 만든다. 게다가 *Mini Shai-Hulud* 악성 패키지들은 **유효한 SLSA provenance 를 달고 있었다**(「증명이 있다」≠「증명이 뜻하는 바가 참이다」). 실제 방어선은 **lockfile integrity** 뿐이고 그것만 문다(6 repo 실측 24/24 고정). 최초 해석(TOFU)은 두 번째 증인이 없어 **미검사**. 2026-08-24 +2: ⓪ `verify:debrief-sink` — 영속 싱크의 **모든 줄**이 카드 계약을 만족하는지. CLI 는 쓰기 시점에 검증하는데 싱크는 git 의 평범한 jsonl 이라 **뒷문이 열려 있었고**, 그 뒷문을 쓴 것이 가장 성실한 저자들이었다 — `opus-5`·`verified`·커밋 ref 를 단 카드 4장이 `option`(계약은 `alternative`)·배열 `artifacts` 로 **조용히 폐기**됐다. ⚠ 그 폐기는 어디서도 빨갛지 않았다: `curate` 가 *"invalid 4개"* 를 stderr 로 말하고 **exit 0** 을 냈고 **개수만 말하고 좌표를 안 줘** 읽어도 못 고쳤다(경보 수명 3주). 이제 file:line+필드 사유로 지목한다.

---

## 2026-08-24 — 백그라운드 작업의 «exit code 0» 이 `tail` 의 것이었다

**실측.** 지식 재색인을 이렇게 띄웠다:

```sh
timeout 3000 bun run knowledge:ingest:rag -- --apply > /tmp/rag-apply.log 2>&1
RC=$?; echo "INGEST_EXIT=$RC"; tail -22 /tmp/rag-apply.log
```

600s 에 하네스가 백그라운드로 옮겼고, 완료 알림이 왔다:

    <status>completed</status>
    Background command "Apply knowledge RAG ingest" completed (**exit code 0**)

**그런데 출력 첫 줄이 `INGEST_EXIT=124` 였다** — `timeout` 이 50분에 죽였다는 뜻이다.
알림의 `0` 은 복합문 **마지막 요소인 `tail` 의 종료코드**다.

실제 상태: **33/37 처리 · 에러 0 · 마지막 줄 `Terminated`**.

### 왜 이게 새로운 자리인가

이 저장소는 「파이프 뒤의 `$?` 는 남의 것」을 이미 알고 있었고 그 문장이 여러 곳에 적혀 있다.
새로운 것은 **판정 주체가 내가 아니라 하네스**라는 점이다:

| | 판정하는 쪽 | 무엇을 읽나 |
|---|---|---|
| 내가 셸에서 | 나 | 내가 `$?` 를 어디서 잡는지 통제한다 |
| **백그라운드 작업** | **하네스** | **복합문 전체의 종료코드** — 내 의도와 무관 |

즉 «백그라운드로 넘어갈 수 있는» 명령에서는 **요약·표시용 꼬리를 붙이는 것 자체가 판정을
오염시킨다.** 초록 알림이 실패를 덮는다.

### 규칙

- **백그라운드로 띄울 명령은 판정 대상을 마지막에 둔다.** `; tail`·`; echo` 를 붙이지 않는다.
- 요약이 필요하면 **다음 호출에서** 로그를 읽는다.
- ⚠ **알림의 exit code 를 결과로 읽기 전에 출력 첫 줄과 마지막 줄을 본다.**
  `Terminated`·`Killed`·`INGEST_EXIT=124` 같은 흔적이 거기 있다.

### 함께 밟은 것 — 색인 중에 색인 대상을 편집했다

진행 상황을 원장에 적고 커밋했는데, **그 원장이 색인 대상이었다.** 재실행 시 남은 파일이
4개가 아니라 5개인 이유가 그것이다. 「게이트가 도는 동안 트리를 편집하지 않는다」와 같은 축 —
**색인·스캔도 working tree 를 본다.**


## zsh 에서 `${PIPESTATUS[0]}` 는 **빈 문자열**이다 (2026-08-24 실측)

파이프 왼쪽의 종료코드를 읽으려고 이렇게 썼다:

```sh
bun test scripts/__tests__/uses-edge-gate.test.ts 2>&1 | tail -5; echo "exit=${PIPESTATUS[0]}"
#   → exit=            ← 숫자가 아니라 **아무것도 안 나온다**
```

이 워크스테이션의 셸은 **zsh** 이고 zsh 의 배열은 **1-기반**이며 이름도 다르다 —
`$pipestatus[1]` 이 bash 의 `${PIPESTATUS[0]}` 다. bash 문법을 그대로 쓰면 **오류도 안 나고
빈 문자열**이 된다.

⚠ 위험은 빈 문자열이 **판정에 쓰이는 순간**이다. `[ "$x" = 0 ]` 은 거짓이라 그나마 시끄럽지만,
`[ -z "$x" ] && commit` 이나 `RC=${PIPESTATUS[0]:-0}` 같은 관용구는 **판정 불능을 0(초록)으로
접는다.** 이 파일의 §「판정 불능과 위반은 다른 종료 코드여야 한다」와 정확히 같은 축인데,
여기서는 **셸 문법 하나가** 그 구분을 지운다.

처방은 파이프를 안 쓰는 것이다 — 판정할 명령을 **단독으로** 돌려 `$?` 를 읽고, 출력이 필요하면
파일로 받는다:

```sh
bun test scripts/__tests__/uses-edge-gate.test.ts > /tmp/t.log 2>&1; RC=$?
tail -5 /tmp/t.log
```

**같은 세션에서 검산한 부수 사실**: `bun test` 는 `N pass · 0 fail · **1 error**` 에서
**exit 1** 을 낸다(실측: `1 pass · 0 fail · 1 error` → exit 1). 즉 러너는 정직했고
**거짓 초록은 러너가 아니라 내 계측에서 나왔다.** 「테스트가 안 돌았는데 초록」의 두 번째
사례였는데, 이번 원인은 `it`/`test` 미import 로 **파일 등록이 깨진 것**이었고 그 신호는
출력의 `1 error` 한 줄뿐이었다 — 그 줄을 `tail` 로 보긴 봤으나 **종료코드는 못 읽었다.**

## 2026-08-25 — 게이트 배선 검사를 **두 번째로** 만들었다 (그리고 약한 쪽이 진실이었다)

멤버(`modfolio-connect`)의 `unwired-gates` 가 허브가 새로 실은 `verify:supply-chain` 을 잡았다.
*"Not wired is a fine answer; not deciding is not."* 옳은 말이라 **허브에도 만들었다.**
만들고 배선하고 대조쌍까지 붙인 뒤에, 허브에 **이미 같은 검사가 있다는 것**을 알았다 —
`scripts/__tests__/gate-wiring-coverage.test.ts`, 2026-08-05 작성, 사유 목록 15항목,
그리고 **같은 오탐들을 이미 겪고 주석으로 적어 둔** 파일이었다.

그 세션의 주제가 *「부품이 있는데 아무도 안 쓴다」* 였다. 진단하던 사람이 같은 날 저질렀다.

### 두 벌이면 **약한 쪽이 조용히 진실이 된다**

합치기 전, 두 구현의 배선 판정이 달랐다:

| | 이름 | 경로 | basename | **인자** | 자기 파일 |
|---|---|---|---|---|---|
| 기존 테스트 | ✓ | ✓ | ✓(부분매칭) | ✗ | ✗ |
| 새 CLI | ✓ | ✓ | ✓(경계) | ✓ | ✓ |

기존 쪽의 basename **부분매칭**이 `pulse.ts` 를 `session-start-evolve-pulse.ts` 에 걸어
**한 번도 돌지 않은 검사를 「돈다」고** 보고하고 있었다. 그리고 그 판정이 테스트에 있었으므로
**그게 강제되는 진실**이었다. 새 구현이 더 정확했지만 강제력은 낮은 쪽에 있었다.

→ 합쳤다: **판단 하나**(`judgeUnwired`·`resolveRunsBy`) · **사유 하나**(`INTENTIONALLY_MANUAL`) ·
**입구 둘**(테스트=강제 · CLI=사람이 읽음). JSON 설정 파일은 지웠다(사유가 두 곳이면 같은 문제다).

### 이 검출기가 개발 중 낸 오탐 **네 가지 — 넷 다 검출력을 0으로 만드는 방향**

| # | 무엇 | 왜 위험한가 |
|---|---|---|
| ① | 이름만 찾아 **경로 호출**을 놓쳤다 | 정상 배선이 「아무도 안 돌린다」 — 오탐이 채널을 죽인다 |
| ② | 경로 **전체**로 찾아 `join('a','b','c.ts')` 조립을 놓쳤다 | 같은 방향 |
| ③ | basename **부분매칭** (`pulse.ts` ⊂ `…-pulse.ts`) | 안 도는 것을 「돈다」 — **검사가 사라진다** |
| ④ | 실행처 스캔이 **스크립트 자기 소스**를 읽고 자기 이름을 찾았다 | 같은 방향. `pgrep -f` 자기 매칭과 동형 |

⚠ **그 오탐이 사람이 쓴 「사유」까지 오염시켰다.** 나는 ③④ 가 살아 있는 출력을 믿고
*"이건 안 돌아서 뺐다"* 는 사유를 **셋 지어냈고 둘이 사실이 아니었다.** 계측기가 틀리면
그 위에 쓰인 산문도 함께 틀린다 — 그리고 산문은 다음 사람이 **검산하지 않는다.**

### 「전부 배선」은 답이 아니다 — 그러나 비용은 **줄일 수 있다**

`verify:uses-edge` 가 체인 밖에 있던 이유는 **43초**였다. 그런데 그 43초의 대부분은
`consumer:census` 스폰(≈27s)이었고, **그 결과는 판정에 쓰이지 않았다** — 사람이 읽는
정보 한 줄에만 쓰였다. 판정과 관측을 분리하니 **86ms** 가 됐고 배선할 수 있게 됐다.

> **판정과 관측을 한 명령에 섞으면, 관측의 비용 때문에 판정이 체인 밖에 머문다.
> 그리고 밖에 있는 검사는 틀린 채 오래 산다.**

반대 방향도 있었다: `verify:tools` 를 배선했다가 **되돌렸다.** 기존 파일이
*"외부 HTTP 를 쏜다 · 스스로 advisory 라고 말한다 · 거짓 빨강은 노이즈보다 나쁘다"* 라고
**이미 적어 두었는데 읽지 않고 배선했다.** 읽지 않은 것이 잘못이지 그 판단이 틀린 게 아니다.

### 부수 사고 — 추출이 **무한 재귀**를 만들었고 typecheck·CLI 가 둘 다 초록이었다

블록을 함수로 승격하면서 치환이 **잘못된 쪽**을 잡아 함수가 자기를 부르게 됐다.
`tsc` 는 재귀를 오류로 보지 않고, **CLI 는 main 에 원본 블록이 남아 있어 정상 동작**했다.
드러난 곳은 그 함수를 **라이브러리로 부른 테스트** 하나뿐이었고, 증상은 실패가 아니라
**무응답**이었다(560초 타임아웃). 「초록 두 개가 있어도 세 번째 소비자에서 처음 드러난다」.

## 2026-08-25 (2) — 게이트의 **관문**이 판정보다 먼저 틀린다

`verify:host-scan` 은 「저장소 밖을 도출하는 스크립트가 설치본 가드를 갖는가」를 본다.
그날까지 **초록**이었고, *"저장소 밖을 도출하는 파일은 모두 설치본 가드를 가집니다"* 라고
출력하고 있었다. 그런데 그 주에 만든 fleet 스캐너 셋이 가드 없이 저장소 밖을 잡고 있었다.

판정 로직은 멀쩡했다. 틀린 것은 **관문 한 줄**이었다:

```ts
if (!source.includes('import.meta.url')) continue;   // ← 여기서 탈락
```

이 저장소의 가장 흔한 관용구는 `resolve(import.meta.dir, '..', '..')` 이고 **33파일**이
그걸 쓴다. 그 파일들은 파싱에 **도달하지 못했다.** 관문을 넓히자:

```
위치 기준 도출  14파일 → 29파일
위반            0건   → 3건   (consumer-census · part-adoption · uses-edge-gate)
```

셋 다 `files` 로 게시본에 실려 33 repo 로 가는 파일이다. 설치본에서 돌면
`resolve(root, '..')` 가 형제 워크스페이스가 아니라 **`node_modules/@modfolio/`** 가 되고,
읽기 도구는 **대상 0건을 「위반 없음」으로** 낸다.

> **「빈 대상은 통과가 아니라 실패다」의 간접 형태 — 대상이 0이 아니라 «좁았다».**
> 0건은 눈에 띈다(양성 대조가 잡는다). **좁은 대상의 «전부 통과»는 안 띈다.**
> 이 게이트에는 `withDerivation === 0` 이면 판정 불능이라는 양성 대조가 **있었고**,
> 14파일이 잡혔으니 그 대조는 **통과했다.** 양성 대조는 «0인가»만 보지 «충분한가»는 안 본다.

부수로 드러난 것: 같은 파일에 `main()` 이 **top-level 무조건 호출 + `process.exit(0)`** 이었다.
그래서 대조쌍 9건을 붙였는데 `bun test` 가 **exit 0** 을 냈다 — 출력에 `N pass` 가 없고
게이트의 스캔 결과만 있었다. **테스트가 한 줄도 돌기 전에 그 모듈이 프로세스를 끝냈다.**
러너는 «0 pass» 를 실패로 보지 않는다. → `verify:importable` 신설, 18파일 수정
(CLI 동작 불변 — `bun run <file>` 은 `import.meta.main` 이 참이다).

⚠ 그 게이트의 **초판이 19건 과탐**이었다: 들여쓰기를 안 봐서 함수 안의 `main()` 호출까지
top-level 로 셌다. **과탐은 규약 준수를 벌하므로 채택을 직접 막는다** — 컬럼 0 요구로 좁혔고,
대조쌍에 「함수 안의 호출」을 넣어 그 축을 잠갔다.

## 2026-08-22 — `capability:ledger`: 손으로 센 숫자가 **전부** 틀렸다

(2026-08-25 에 CLAUDE.md 에서 이관 — 원문 그대로. 상시 주입 산문은 라우터로 보낸다.)

canon `capability-ledger.md` 의 「어느 역량을 누가 갖고 부품이 있는가」를 `ecosystem.json`
미러와 대조하는 게이트다. 계기는 **내가 손으로 센 숫자가 전부 틀렸다**는 것이다 —
계획 초안이 「역량 32 · 주인 있음 12」라고 적었는데 같은 목록을 프로그램으로 세니 **45행**,
배정까지 합쳐 **50**, 주인 표시 15개 중 **6개는 부품이 0개**였다.

*"주인 있음 12"* 는 «그만큼은 됐다» 로 읽히므로 그 숫자로 물결 규모를 정하면
**실제보다 작게 잡는다.**

⚠ **첫 구현의 역방향 검사가 건전하지 않았다** — `claimed` 인데 부품>0 이면 실패로 썼더니
**정상 입력에서 14건**이 떴다(connect 의 부품 3개는 「인증·SSO」의 것이지 「RBAC」의 것이
아니다). 같은 파일이 *"어느 부품이 어느 역량의 것인지는 원리적으로 안 보인다"* 고
**적어 놓고** 그 축을 검사했다. 역방향을 **repo 단위로 내려** 건전하게 만들었고 teeth 는
유지된다(부품을 냈는데 원장이 전부 claimed 면 실패). 음성 대조 4종 전부 exit 1, 정상 exit 0.

---

# 게이트 신설 이력 — CLAUDE.md 에서 이관 (2026-08-25)

> **왜 옮겼나.** `quality:all` 표 셀 하나가 **9,994 bytes** 까지 자랐고, 그게 30+ repo 에
> **매 턴** 주입됐다. 내용은 전부 진짜였다 — 틀린 것은 **운반 방식**이다.
> 이 저장소가 같은 날 세 번 그 벽에 부딪혔다(`context:budget` 이 커밋을 세 번 막았다).
>
> 이제 CLAUDE.md 에는 **단계 수와 포인터**만 둔다. 게이트가 무엇을 하는지는
> `bun run codex:search "<게이트 이름>"` 으로 찾고, 왜 생겼는지는 여기서 읽는다.
> 각 게이트의 **파일 상단 주석**이 1차 출처다 — 그게 코드와 함께 움직이기 때문이다.

2026-08-25 +8: ⓪ **`codex:gate`** — 지식 코퍼스 **440편을 링크된 인덱스로 컴파일**하고 Error Book 을 본다. 계기: 상시 주입 **103,692 bytes(예산의 94.3%)** 로 포화 — 새 교훈이 옛 교훈을 밀어내야만 들어가는 상태였다. 3단 점진 공개(L0 `ROUTER.md` 2.4KB · L1 `codex_search` · L2 본문) + MCP 2종. frontmatter 는 **이미 있었다**(124/124 canon) — 만든 게 아니라 컴파일했다. ⚠ 치명(주소충돌·끊긴링크·형식오류)만 실패로 세고 나머지(orphan 65·no-routes 77)는 **수치로만** — 상시 빨강은 무시를 훈련시킨다. ⚠ **여덟 번 틀렸고 전부 코퍼스가 아니라 내 컴파일러였다**(SKILL.md 50편 주소 접힘 · journal 과탐 176 · `description` 미독 · 괄호 안 쉼표 분리 · ADR 번호 정체성 · `010`/`010a` 접힘 · archive 미색인 · 파싱실패를 내용결함으로). 대조쌍 17건이 사건별로 잠근다. 검색은 홀드아웃 **1/5 → 4/5**(개선분 2건은 **정답 라벨 정정**이고 스코어러 수정은 1건 — 그 구분을 테스트에 적었다). 2026-08-25 +7: ⓪ **`verify:importable`**(top-level `main()`+`process.exit()` = import 하는 테스트가 **0 pass 로 초록**. 18파일 수정·CLI 불변) ⓪ **`verify:host-scan` 관문 수정** — `import.meta.url` 담은 파일만 보고 나머지를 파싱 전에 건너뛰어 `import.meta.dir` 33파일을 통째로 못 봤다. 대상 **14→29**, 위반 **3건**(전부 그 주에 만든 fleet 스캐너). **「빈 대상은 실패다」의 간접 형태 — 0이 아니라 좁았다.** 서사 = 증례 `evidence-cases-gates.md`. 2026-08-25 +6: ⓪ **`gates:unwired`**(아무도 안 돌리는 검사 · 배선 44 · 사유 있는 미배선 11) + 함께 배선 5종. ⚠ **허브에 이미 같은 검사가 있었다** — 「부품이 있는데 아무도 안 쓴다」를 진단하던 세션에서 저질렀다. 합쳤다(판단 하나·사유 하나·입구 둘). ⚠ `verify:uses-edge` **43s→86ms** — 판정에 안 쓰이는 관측을 `--full` 로 분리했다. **판정과 관측을 섞으면 게이트가 체인 밖에 머물고, 밖에 있는 검사는 틀린 채 오래 산다.** 서사(오탐 4종·자기매칭·무한재귀) = 증례 `evidence-cases-gates.md`. 2026-08-24 +5: ⓪ `release:sbom` — 게시본 부품 명세를 lockfile 에서 결정적으로. ⚠ **U4 넷 중 이것만 만들었다** — OTel·CloudEvents/AsyncAPI·OpenFeature 는 fleet 채택이 **전부 0**이고, 오늘 실측이 「부품 54종 중 프로덕션 소비 4종」이라 표준을 더 만들면 **소비자 없는 것이 네 개 더** 생긴다(§13). 만들지 않은 셋은 `sbom-gate.ts` 의 `NOT_BUILT` 에 **실측과 재검토 트리거**로 남겼다. ⚠ SBOM 은 provenance 도 서명도 취약점 판정도 **아니다** — 그 셋을 산출물 안에 적어 「공급망 안전」 오독을 막는다. 서사 = 증례 `evidence-cases-gates.md`.. 2026-08-24 +4: ⓪ `verify:retired-approach`(폐기 접근이 «진행 중» 으로 살아 있는가 — README 가 **AI 에이전트 독자용**이라 적어 놓고 **금지된** GHA 배포를 두 달간 «🔲 대기» 로 가리켰다) · ⓪ `verify:supply-chain`(`@modfolio/*` lockfile integrity — 요점은 초록이 아니라 **못 보는 축을 매번 같이 찍는 것**: Forgejo 는 서명·provenance 를 주지 않는다) · ⓪ `verify:debrief-sink`(영속 싱크 전 줄이 카드 계약을 만족하는가 — 검증하는 정문 옆의 **잠기지 않은 뒷문**, 검증된 프론티어 카드 4장이 3주간 조용히 폐기됐다). **셋 다 서사·함정·규칙을 세 번 고친 이력 = 증례 `evidence-cases-gates.md` §2026-08-24.** ⓪ `verify:cf-scope` — repo 별 **최소 CF 권한**을 `wrangler.jsonc` 바인딩에서 도출(계획 §14 #2). 메가토큰은 **353 권한·무기한**인데 실제 필요는 repo 당 1~8. 서사·함정 = 증례 `evidence-cases-gates.md`. ⓪ `verify:user-facing` — **「사용자 0」을 가정하지 않고 잰다**. 속도 doctrine 전체가 그 가정 위에 있는데 **계측기가 없었다**(사용자 필드 0 · harness-lock 0/33 · liveHosts 전부 빈 배열). 신호가 있어도 실패하지 않는다(앱 단위 판단). 서사 = 증례 `evidence-cases-gates.md`. ⓪ `registry:check` — 신선도. `verify:registry` 는 **내적 정합만** 봐서 22 부품 stale 을 초록으로 통과시켰다. ⓪ `verify:bunx-leak` — `bunx <tool>` 이 로컬 의존성을 우회하는가. 이 WSL 에서 **`bunx` 는 Windows npm shim** 이라 `quality:all` 이 두 단계에서 죽었다. CLAUDE.md 가 그 현상을 *"404 나면 재시도"* 로 **원인을 잘못 짚고 있었다**. 서사 = 증례 `evidence-cases-gates.md`. ⓪ `ai:census` — **런타임 LLM 호출 인구조사**. *「AI 가 코드를 쓴다」≠「결과물이 AI 를 요구한다」*를 검사 가능하게. 실측 **65 지점/13 repo**. 허브만 red. ⚠ 개발 중 **자기 자신에게 네 번** 걸렸다 — 서사 = 증례 `evidence-cases-gates.md`. ⓪ `data:substrate` — db 보유 24항목의 `dataSubstrate` 선언·내적정합·self-hosted drill 요구 ⓪ `quota:scan` — Neon 무료한도 70%/100% 임계(무자격 = SKIPPED 명시 — «안 쟀다»≠«안전») ⓪ `stack:conformance` — 선언 스택 vs 실물(unocss 제보의 정공법 · 허브만 red, sibling 은 advisory. 첫 실행이 Loom 미러 'Hono' 허위와 Astro7 전환 미반영 6곳을 잡았다). 서사는 canon `modfolio-db.md`·ADR-022. / 2026-08-23 +2 — 서사는 canon **`instruction-drift.md`** · **`judgment-core.md`** 에 있다(이 표에 다시 쌓지 않는다 — 그 문서들이 기록하는 사건이 정확히 「지시문이 자란다」이다): ⓪ `context:budget` — 상시 주입 산문 상한(총 110KB·단일 40KB). 실측 **55,426 → 206,964 (4주 3.7배)** · `agent-evidence.md` 단독 **3,068 → 137,065 (6주 44배)** · 그건 `UNIVERSAL_RULES` 라 **30+ repo 매 턴**. 처방은 삭제가 아니라 **라우터**(검사 가능→게이트 · 휴리스틱→Muse · 실측 서사→`evidence-cases-*.md`, 셋 다 매 턴 비용 0). 137KB 는 **한 글자도 안 버리고** 무손실 이관, 총 **207→98KB**. 과거 적용 시 최초 발화 **2026-08-04 = 19일 늦었다**. ⓪ `part:spec` — 판단 코어 규격(I/O 0·의존⊆zod·대조쌍·provides). ⚠ 초판이 모집단을 틀려 **19개 중 13개가 빨갛고 그 대부분이 정당**했다(`pay-sdk` 의 `fetch()` 는 존재 이유 — `assembly-law` §1 은 3표면을 **메뉴**로 준다). ⚠ 회귀 테스트가 **죽은 검사**를 잡았다 — 문자열 스트립이 `from "node:fs"` 경로까지 지워 `node:` 검사가 한 번도 발동한 적 없었고 **라이브에서는 «위반 없음»** 으로 보였다. / 2026-08-22 +1: ⓪ `capability:ledger` — 원장 ↔ `ecosystem.json` 미러 대조. 계기는 **손으로 센 숫자가 전부 틀렸다**는 것(「32·12」로 적었는데 실측 45/50, 주인 15 중 6은 부품 0). ⚠ 첫 구현의 역방향 검사가 **정상 입력에서 14건** 오탐 — 같은 파일이 「원리적으로 안 보인다」고 적어 놓고 그 축을 검사했다. 서사 = 증례 `evidence-cases-gates.md`. / 2026-08-17 +4: ⓪ `verify:e2e-naming` — Playwright `testDir` 안의 파일이 config 의 `testMatch` 를 만족하고 bun 의 테스트 패턴과 안 겹치는지. `.spec.ts` 는 **최악의 조합**이다 — Playwright 는 `testMatch: **/*.e2e.ts` 라 **안 잡고**(조용한 미실행), bun 은 claim 해서 `harness-publish` 의 bare `bun test` 를 죽인다(**릴리즈 차단**). ⚠ **재발이었다**: `e89a2c42`(2026-08-07)가 `pages.spec.ts → pages.e2e.ts` 로 같은 것을 고쳤는데 10일 뒤 `playwright init-agents` 산출 `seed.spec.ts` 가 `git add -A` 로 딸려 들어와 릴리즈를 막았다 — 규약은 있었고 게이트가 없었다. 생성 에이전트 지시문도 `.spec.ts` 를 가르치고 있어 함께 정정. ⓪ `verify:host-scan` — ⓪ `verify:host-scan` — `scripts/` 는 `files` 로 **게시본에 통째로 실려 33 repo 로 간다.** 그런데 스크립트 위치 기준으로 저장소 **밖**(형제 워크스페이스)을 잡는 관용구(`join(ROOT,'..')`)가 있고, 설치본에서 돌면 그것이 `node_modules/@modfolio/` 가 된다 — 읽기 도구는 **대상 0건을 «위반 없음» 으로** 내고 쓰기 도구(`sync-npmrc`)는 **npm 패키지에 쓴다**. `isPackagedBundle` 은 정확히 이것 때문에 2026-04-25 modfolio P0 로 만들어졌는데 **워크스페이스를 도출하는 5파일 중 가드를 가진 것이 0개**였다(헬퍼는 쓰기 경로 6곳에만 붙어 있었다). ⚠ 손 스윕은 4개만 찾았고 **5번째(`registry/connect-drift.ts`)는 이 게이트가 찾았다.** ⚠ 첫 구현은 `includes('isPackagedBundle')` 라 **내가 쓴 주석만으로 통과**했다 — 「언급만으로 통과한다」(pay). 호출 형태 + 주석 배제로 좁혀 대조쌍 통과. ① `citation:hygiene` — canon 의 「핵심 인용」이 1차 출처 링크와 **원문 대조 날짜**를 갖는지. 계기는 `multi-agent-research-pattern` v1.0.0 이 **원문에 없는 문장**을 3개월간 fleet 에 배포했고 방향이 정반대였다는 것 — 우리는 「80% 절감」, 원문은 「15× more tokens」. ⚠ 이 게이트는 «문자열이 원문에 실제로 있는가» 는 **못 본다**(미검사 축), 대조 증거의 존재만 본다. ② `verify:retired-canon` — 은퇴 선언 목록 ↔ hub 실물(`sync_to_siblings:false`) 대조. 틀리면 대가가 비대칭이다: 오타는 조용한 무력화지만, **아직 배포 중인 canon 을 은퇴시키면 멤버의 현행 지침이 폐기 stub 으로 덮인다**(pdgd 제보 2026-08-16 ②). / 2026-08-15 +1 = `atlas:gate`. 